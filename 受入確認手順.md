# 受入確認手順 — Jev適用 Phase 0 / 1 / 2 と Web Speech 共有音声修正

対象ブランチ `claude/kind-dijkstra-y879gv` / 2026-09-25 / HTML v1.49.19 / 拡張 1.4.30

この変更の受入は2段構えです。**A は私が実行済み**で、`node` を叩けば再現できます。
**B は実機でしか確認できない項目**で、こちらをお願いします。

---

## A. 自動テスト（実行済み・456件）

```
node extension/tests/run-all.cjs                                       # 決定論的スイート 443件
NODE_PATH=$(npm root -g) node extension/tests/run-all.cjs --with-e2e   # ＋実ブラウザE2E 13件
```

|スイート|件数|守っているもの|
|---|---|---|
|`test-turn-decision`|69|OFF互換、INV-08/09/**10（有界の待ち：待つ相手・上限・0で無効）**、モード上限クリップ、フロアのタイマー条件、契約（silenceMs の null）、候補の安全性、プライバシー、prosody peek の非破壊性、訂正率の判断源別集計|
|`test-turn-providers`|80|経路レジストリの契約完全性、**経路の既定 model がそのまま assist／active で使えること**（既定が alias だったため実機で送信が全件拒否されていた）、応答正規化の拒否条件、circuit breaker、経路別キー、到達不能／HTTPエラー／中断の区別、probe が解析成功で初めて ok になること|
|`test-tts-limits`|27|OpenAI音声の上限ヘッダの読み取り、429を他の失敗と分けること、待ち時間を捏造しないこと、上限ヘッダが無い応答では記録しないこと。**Aivis の「予測（こちら側の数え）」と「事実（429）」を混ぜないこと**、既定では予測で送信を止めないこと、壊れた設定値が `send` として読まれること、画面の選択肢とコードが一致すること|
|`test-segment-policy`|6|逐次読み上げの入力欄とコードの丸めの一致。**画面で入れられる値がそのまま効くこと**、使える範囲がラベルに書いてあること|
|`test-settings-style`|6|`設定UI設計指針.md` の数値と実際のCSSの一致。入力欄の指定が1か所だけであること、JSが作るパネルに `panel-form` が付いていること、`#id` にフォントサイズが無いこと、長い説明がたたまれていること|
|`test-app-html-sync`|4|`extension/app.html` が `index.html` の変換結果であること。**片方にだけ設定欄を足すと落ちます**（v1.49.11・v1.49.12 で実際に起きた版ずれの再発防止）|
|`test-backchannel`|23|相づちを読まない設定。**落としてはいけないものを落とさないこと**が重点 — 既定では何も落とさない、答えになりうる語（はい／うん／Yes／okay 等）を落とさない、既定の一覧にそれらが混入していない、「え？」を落とさない、一覧の無い言語では何も落とさない、手動再生は落ちない|
|`test-turn-proxy`|26|拡張側の中継。許可外オリジンの拒否、HTTPの拒否、権限が外れたときの停止、ヘッダのallowlist、頼める相手の限定、ステータス保持、打ち切り、鍵と本文が応答に混ざらないこと|
|`test-file-sync`|27|`index.html` と `extension/app.js` の一致、両ファイルの構文|
|`test-release-manifest`|7|動いている版が `versions.json` に載り、記録された SHA-256 がファイルと一致すること。HTML と拡張の対応、README の存在|
|`test-turn-trace-replay`|18|replayer が Rules の決定を再現できること、自動ラベルの窓|
|`test-input-validation`|10|プリセット3種 × 経路3種の入力源検査|
|`test-teams` 他 既存6|94|Teams・話者帰属（繰越カードの区間を含む）・送出経路・マイク判別・経路ログの重複抑制|
|`test-html-*`（tools/ 3件）|46|`index.html` 側の橋渡し・字幕コマンド・タブ音声。v1.4.9 ではこれがゲート外にあり、壊れた版を通してしまった|
|`test-browser`|—|実 Chromium の WebRTC ループバック。送出バイト > 0|
|`test-settings-ui`|13|判断層の設定UIが実ブラウザで生えること、経路別キーの永続化、キーが画面へ戻らないこと、書き出しに載らないこと|

自動テストで**担保できないもの**は、実際の音声・実際の Teams・実際のネットワークに依存する部分です。それが B です。

---

## B. 実機で確認する項目

### B-1. OFF互換 — 最重要

この変更の前提は「既定では v1.47.1 と同一挙動」です。ここが崩れていたら他は見る必要がありません。

- [ ] **既定のまま通常の会議を行い、従来と区別できないこと。** 字幕の出方、区切り、読み上げ開始の間。
- [ ] **判断層が沈黙していること。** ⚙→音声 の「発話交代の判断層（実験）」の状態行で確認する。
      `判断 off ／ trace off ／ 確定 rules N / provider 0` のように provider が 0 のままであること。
- [ ] 同じことを DevTools でも確認できる。

```js
CFG.turnDecisionMode        // → 'off'
TurnTrace.rows.length       // → 0
Object.keys(TurnDecision.cache).length   // → 0
```

- [ ] **診断ログに `turn-decision-*` が1件も出ないこと。** ⚙→診断 のログ出力で確認する。
- [ ] `segmentMode=adaptive` で会議し、診断の `adaptive-decision` の `correctionRate` が従来と同じ推移をすること。off のあいだは全 commit が rules 由来なので合計値と一致する。

### B-2. Web Speech で共有音声（今回の修正）

- [ ] **Web プリセット + 音声認識=Web Speech + 入力元=タブ／システム音声 で開始できること。** 以前は「Webプリセットの共有音声にはOpenAI / Groq / Geminiの音声認識を選んでください」で止まっていた。
- [ ] Many to Many プリセットでも同じく開始できること。
- [ ] **TTS 送出専用の構成が巻き込まれないこと。** Web プリセットで A も B もマイクにし、会議音声は読み上げの送出先としてだけ使う。これで開始できること。
- [ ] 1 on 1 プリセットが従来どおり動くこと（退行がないこと）。
- [ ] 外部API（OpenAI など）で共有音声を認識する構成が従来どおり動くこと。

**Chrome 135 未満では失敗します。** その場合に出るのは
「共有音声の直接認識を開始できません：… VB-CABLEまたは外部APIを選んでください。」で、
これは**正しい動作**です。開始前に塞ぐのをやめ、実機が理由を出すようにしたためです。
その環境では ⚙→音声 で共有音声の認識経路を VB-CABLE か 外部API にしてください。

### B-3. trace 記録と replayer（Phase 0 の本番投入）

⚙→音声 に「**発話交代の判断層（実験）**」という折りたたみが増えています。DevTools は不要です。

1. 折りたたみを開き、**trace 記録** を `record` にする。
2. **認識した原文も記録する** にチェックを入れる（replayer の再現検査に必要）。
3. 会議を行う。判断モードは `off` のままでよい。
4. **trace を書き出す** を押す。`turn-trace-<日時>.json` が落ちる。
5. 手元で集計する。

```
node extension/tools/turn-trace-replay.js turn-trace-....json
```

- [ ] 折りたたみ内の状態行が、記録中に行数を増やしていくこと。
- [ ] **`replay` の再現率が 100% に近いこと。** ここが低いと以降の数字は読めません。低い場合は原因を調べてから baseline を取ります。
- [ ] `premature` と `firstAudio p50 / p95` が出ること。これが baseline になります。
- [ ] 「認識した原文も記録する」を外して記録すると `raw text not recorded` と報告され、**黙って0点にならない**こと。
- [ ] 記録中でも会議の挙動が変わらないこと（判断モードは off のまま）。
- [ ] **trace を破棄する** で行数が 0 に戻ること。

**プライバシー。** 原文を記録しているあいだ、会議の発言が端末内の trace に残ります。外部へは送られませんが、保管と削除のルールを決めてから有効にしてください。

### B-4. 停止・再開とセッション分離

- [ ] 停止→再開を数回繰り返し、字幕が前のセッションと混ざらないこと。
- [ ] 再開後に `Object.keys(TurnDecision.cache).length` が 0、`TurnTrace.rows.length` が 0 に戻ること。

### B-5. 拡張版

`index.html` と `extension/app.js` は手作業同期です。自動テストで文字列一致は見ていますが、実機での等価性は別です。

- [ ] `chrome://extensions` →「パッケージ化されていない拡張機能を読み込む」で `extension/` を指定し、**B-1 と B-2 を拡張版でも通すこと。**

### B-6. 長時間

- [ ] 1時間の会議を通し、文字の欠落・重複がないこと、メモリ増加が許容内であること。
- [ ] `turnTraceMode=record` のとき、trace の行数が上限 40,000 に達して `dropped` が増えていないか確認すること（1時間で達するなら上限を上げます）。

---

### B-7. Jev をアドオン経由でつなぐ（**達成済み**）

**v1.49.6 の実機ログで、Jev が10回投げて10回とも応答し、契約どおり解析できました。**
モデルは `jev-1.13.0`、往復の中央値は約225ms、失敗0。**この項目は完了です。**
以下は再現手順として残します。

**疎通は確定しました。** `tools/probe-jev.ps1` を Windows から実行した結果：

```
-- 判定 -------------------------------------------------
  API は動いています。
  ただし CORS 応答ヘッダがありません。ブラウザからは結果を読めません。
```

**API もキーも契約も問題なく、ブラウザが結果を読めないだけ**でした。サーバが
`Access-Control-Allow-Origin` を返さないので、ブラウザは応答を破棄して
`TypeError: Failed to fetch` を投げます。**ブラウザからの直叩きは成立しません。**
v1.49.1 で立てた「余計なヘッダが preflight を止めている」という仮説は外れでした。

拡張は、利用者が許可したホストに対してこの制約を受けません。そこで往復だけを
拡張に肩代わりさせる経路を足しました（v1.49.10 / 拡張 1.4.21）。

- [ ] **まず probe をもう一度走らせる。** スクリプトを直しました。**前の版は状態コード
      だけを見ていて、中身が契約どおりかを見ていませんでした**（アプリ側の疎通ボタンで
      直したのと同じ欠陥です）。新しい版は `answers` の4つが揃っているかまで確かめ、
      **答えたモデルの version・実測トークン・境界の判断**まで出します。
      `probe-jev-body.json` も新しいものに差し替えてください。

      ```
      -- 解析 -------------------------------------------------
        答えたモデル : jev-1.13.0
        入力トークン : ...
        境界の判断   : ...（confidence ...）
      ```

      **ここが出れば、契約が通っていることが protocol の段で確定します。** 残るのは
      「ブラウザで解析できること」だけになります。

- [ ] **アドオンを 1.4.30 へ更新する。** 最新版ZIPを**空のフォルダー**へ展開し、
      `chrome://extensions` で読み込み直します。同じフォルダーへ上書きすると
      popup が版の不一致を検出して全機能を止めます（これは意図した二重管理です）。

- [ ] **ポップアップで接続先を許可する。** 「判断層（Jev）の接続先」に
      `https://api.typesafe.ai` を入れて**「この接続先を許可」**を押し、Chrome の
      許可ダイアログで許可します。「判断層の中継を許可済み」と出ればここは完了です。

- [ ] **経路を切り替える。** HTML 本体の ⚙→音声→判断層 で
      **「Jev — アドオン経由（CORS回避）」**を選び、TypeSafe のキーを入れます。

- [ ] **疎通を試す。** 同じ画面の**「この経路へ疎通を試す」**を押します。
      判断モードは `off` のままでも押せます。

|表示|意味|次にやること|
|---|---|---|
|到達しました（N ms）|**使えます**|B-8 へ|
|アドオンを経由できません|HTML本体がアドオンに未接続|ポップアップの「HTML本体を開く」で開き直す|
|アドオンの接続先が未許可です|上の許可がまだ|ポップアップで許可する|
|サーバまでは届きましたが拒否されました（HTTP 401）|キーの問題。中継は成功している|キーを確認|

- [ ] **拡張版でも試す。** ポップアップの「HTML本体を開く」ではなく、アドオン内の
      ページで動かす場合は内容スクリプトを介さず直接つながります。どちらでも
      「到達しました」になることを確認してください。

**ここで「到達しました」と出て初めて、Jev が使える状態です。** 直叩きしか無かった
あいだ、判断層は一度も応答を受け取っていません。

### B-8. 到達したあとに初めてやること

**閾値と質問文の調整は、B-7 が通るまで凍結しています。** 通らない状態で調整しても
体感は変わらないためです。通ったら以下の順で進めます。

**Jev が Rules より良い判断をした実例（v1.49.7）。** 英語→日本語で10回中9回一致、
不一致1回。その1回で Jev は `HOLD` を選び、Rules は切りました。切った結果、次のカードが
「units, Agenda 2063.」だけになり、**「ユニット、アジェンダ 2063。」という意味を成さない
断片が読み上げられました。** 実際の発話は続いていたので **Jev が正解**です。
ただし確率は 0.52 対 0.48、**confidence 0.04 でほぼ五分五分**。どの閾値でも通りません。
**方向は正しいが確信が無い** — これが校正すべき対象です。

**遅延は条件次第で大きく変わります。**

|指標|連続した語り（v1.49.5）|自然な間のある演説（v1.49.7）|
|---|---|---|
|`L3Ms` 中央値|約9.1秒|**約4.2秒**|
|10秒上限で切れた録音|**全部**|**11回中3回**|

**「10秒の録音窓は話が途切れないときだけ効く」**が裏づけられました。会話や演説なら
現状のままでも4秒台です。**STT を替える判断は、実際に使う素材で決めてください。**

**Aivis に注意。** 翻訳が入ると1発話につき1リクエストで、50秒で9回使い
**残り1/10** になりました。**実際の会議では上限（60秒に10回）に当たって待たされます。**

- [ ] **OpenAI 音声の自分の枠を実測する（v1.49.9 で追加）。** ⚙で読み上げを OpenAI に
      して1回鳴らすと、診断の **「OpenAI音声の上限（直近応答）」** に
      `リクエスト 499/500（回復 120ms）／トークン …` のように**あなたのアカウントの
      実値**が出ます。OpenAI の上限は usage tier で決まるので**公表値では分かりません。**
      Aivis の「課金モード（直近応答）」と2行並べると、**どちらが先に詰まるか**が見えます。

- [ ] **上限フォールバックを確かめる（v1.49.10）。** 上限に達した以降は
      **待たずにブラウザ内蔵音声で読みます。** 診断ログの
      `segment tts-rate-fallback` と `tts browser-start` が対で出ます。
      Aivis を使い切るまで喋り続けて、**声が切り替わっても読み上げが止まらないこと、
      待ち行列が伸びないこと**をご確認ください。

      |プロバイダ|上限中|
      |---|---|
      |Aivis|**設定次第**（下の項目）。Aivis が 429 を返したときはブラウザ内蔵音声|
      |OpenAI|429 を受けてから一定時間はブラウザ内蔵音声。**v1.49.9 までは音声を捨てていました**|

- [ ] **Aivis の超過設定を確かめる（v1.49.11・前版の不具合の修正）。**
      前版は「60秒10回」を**こちら側で数えて**10回で Aivis への送信をやめていました。
      **お客さまのアカウントは超過分をクレジットで払う設定なので、これは誤りでした**
      （前回のログで12回、`Microsoft Ayumi` に替わっていました）。

      ⚙→音声→Aivis の**「60秒に10回を超えたとき」が「そのまま送る」（既定）**に
      なっていることをご確認ください。そのうえで、

      - **10回を超えても Aivis の声のままであること**（`tts-rate-fallback` が出ないこと）
      - 診断の **「Aivis 60秒10回を超えたとき」**の行に
        `そのまま送る（…） ／ 直近60秒 12回 ／ 送信可能` のように出ること
      - **Aivis が実際に 429 を返した場合**は `tts-rate-fallback` の `why` が
        `aivis-server-limit` になり、その発話が**捨てられずブラウザ内蔵音声で読まれること**

      をご確認ください。**もし 429 が出るようなら、クレジット側の設定を見直す必要が
      あるということです**（こちらの数えではなく Aivis の返答で分かります）。

      課金を増やしたくない日は「**ブラウザ内蔵音声で読む**」（前版の動き）、
      声を替えたくない日は「**枠が空くまで待つ**」（v1.49.9 以前の動き。**読み上げが
      数十秒遅れます**）を選べます。

**Jev の初回実データ（v1.49.6）で分かったこと。**

- **信頼度の式 `(N×p_max−1)/(N−1)` が10件すべてで厳密に一致**しました。そして
  **N 依存の逆転が実際に出ました**（p_max 0.50・N=3 → 0.24 と、p_max 0.47・N=4 → 0.29）。
  **confidence に閾値を置くと候補の数で判定が動きます。**§6.4 の判断は正しかったです。
- **Jev は Rules より後ろで切りたがります。** 10件のうち不一致は4件で、**4件すべてが
  同じ方向**（`C_STABLE`＝確定済み全文で切る）でした。ばらつきではなく傾向です。
  ただし確率差は小さく confidence も 0.22〜0.30 なので、**どちらが良いかはこのログでは
  判定できません。**
- **費用は約¥4／時間**でした（平均1,470トークン×約420回／時）。以前の見積り
  ¥80〜120 は throttle が上限まで働く前提で、実際の頻度は20分の1でした。

- [ ] **shadow で1本回す。** ⚙→音声→判断層のモードを `shadow` にします。字幕と
      音声の挙動は Rules のまま変わりません。診断の**「判断層の往復」**行
      （順序 29.4）で `要求 / 応答 / 解析失敗 / 到達不能 / 適用` を見ます。
      **要求と応答が一致し、到達不能が 0 であること。**
- [ ] **model を固定 version に変える。** `active` は alias（`jev-latest`）を拒否します。
      `shadow` のうちに固定 version を入れて、同じ値で通ることを確認してください。
- [ ] **shadow のまま trace を記録する。** `turnTraceMode=record` で会議を1本。
      これが閾値校正の入力になります。ここまで来て初めて調整に意味が出ます。

**model は固定 version を入れてください。** 公式では `jev-latest` も `jev-preview` も
現在 `jev-1.13.0` を指しますが、alias は新版が出ると**こちらの変更なしに動きます。**
`active` では両方を弾きます。応答の `model` フィールドが実際に答えた version を返すので、
診断でそれを確認できます。

**日本語は公式が「英語と同等ではない」と明記しています**（CJK は扱えるが精度は劣る、
routing では confidence を注視せよ）。日英の通訳が主用途なので、**日本語側は必ず
shadow で確かめてから active に上げてください。** 言語別に閾値を持つ設計はこのためです。

### B-8b. 実機ログから分かったこと — 主因は判断層ではありません

v1.49.2 の実機ログ（12発話・55秒）を読んだ結果です。

|指標|実測|意味|
|---|---|---|
|STT 応答|発話終了から**約1秒**|**速い。問題ではありません**|
|`L2CommitProxyMs`|0.4〜**11.6**秒（中央値 約6.2秒）|commit から音が鳴るまでの**待ち行列**|
|`L3Ms`|4.5〜12.1秒（中央値 約9.1秒）|発話終了から読み上げ終了まで|

**待ち行列が支配項です。** 1.8倍速に圧縮した読み上げが合計約40秒で、55秒の
セッションの稼働率 73%。しかも最初の録音が10秒上限で切れたぶん**約11秒の初期遅れ**が
でき、稼働率が高いので**一度も追いつきませんでした。**

さらに **12発話のうち4つが5文字以下**（「Hm.」「そう。」「あかんな。」「อึ้ย」）で、
それぞれが Aivis の1リクエストと待ち行列の1枠を使っています。Aivis は
**60秒あたり10リクエスト**が上限で、**50秒で残り0**になりました。

**この3つはどれも境界の判断では直りません。** 効くのは Phase 3 の床（TTS の待ち行列を
見て保持・破棄を決める層）で、そこは未実装のままです。この実機ログは
**境界の精度より床のほうが効く**という証拠になります。

- [x] **判断が要るところ（回答済み・v1.49.12 で実装）。** 短い相づちを読み上げるかどうかは
      仕様の判断でした。①読まない ②次の発話にまとめる ③そのまま読む（現状）の三択に対し、
      **「①③のハイブリッドか選択式に」**とのご指示をいただいたので、**両方**にしました。
      ⚙→音声→「相づちの読み上げ」で3つから選べ、**既定は③（全部読む）のままです。**
      ハイブリッドは真ん中の「つなぎ語だけ読まない」で、**語の一覧**で切ります。

      **①の「文字数で切る」は採りませんでした。** 実測ログの4件で検証すると、3文字以下では
      `Hm.`（落として良い）と `そう。`（**「本当？」への答えなら「はい」と同義**）が同じ扱いに
      なり、`あかんな。`（中身がある）は残ります。**4件のうち1件しか正しく判定できません。**
      文字数で切る設定も3番目として選べますが、**中身のある短文も落ちる**と明記しています。

      **②（次の発話にまとめる）はまだ入れていません。** 確定したカードを保留する必要があり、
      逐次読み上げの待ち行列・話者帰属・議事録に触ります。この設定と同時に出すと、実機で
      おかしかったときどちらのせいか分かりません。ご指示があれば次の版で出します。

### B-8c. 話者帰属を測る前に直したこと

**`startedAt` の引き継ぎバグを直すまで、話者帰属は正しく測れませんでした。**
繰越した末尾カードが元の発話開始時刻を持っていたため、話し続けるほど照合窓が広がり、
**その窓でいちばん長く話していた別人に帰属**していました。v1.49.6 で修正済みです。

- [ ] **Teams で帰属精度を測る（Phase 0 の未測定項目）。** これで初めて、実装の
      バグではなくモデルの精度を測ることになります。診断の
      `speaker-attributed` / `speaker-ambiguous` の比率と、手動訂正の回数を数えます。

### B-8d. 「切り方が短すぎる」件 — Jev との比較結果（**ご判断をお願いします**）

v1.49.10 のログには **Jev の応答が24件**入っていました。`shadow` なので実際の切り方は
Rules です。**同じ入力に対する両者の答えが両方残っている貴重な1本でした。**

|Jev の判定|件数|Rules の動作|
|---|---|---|
|**HOLD（まだ切るな）**|**15**|**15回すべて切った**|
|切る位置に同意|9|切った（一致）|

**15回、Jev は反対しています。うち10回は confidence 0.70 以上。** そして**会話ログを
読み直すと Jev のほうが正しいです。**

- 「Former presidents of」／「the General Assembly.」→ 「元大統領」／「総会。」
  （本来は**「総会の元議長」**。切ったせいで別人になっています）
- 「In recent years,」／「There appears to have been a」／「crisis of confidence.」
  → 真ん中が「**があったようです**」。**1文が3つに割れ、真ん中は単独では意味を持ちません**

**原因は10秒の録音境界です。** 25回の確定のうち **13回**が
`reason:["stt-final","semantic-final-tail"]`。`fourO` の STT は10秒ごとに録音を切って
投げるので、この `final` は**録音の都合で来る final** です。言葉の区切りではありません。

一方、**一致した9回は本当の文末**（「That was 40 years ago.」0.98 など）に集まって
います。**「いつも待て」と言っているのではありません。**

- [ ] **判断が要るところ。** この版では**まだ Jev を切り方に使っていません。** 黙って
      有効にすると、いま積み上げている前後比較の土台が崩れます。お勧めの順番は
      `次期仕様実装状況.md` の「v1.49.10 実測ログでの Rules と Jev の比較」に書きました。
      要点は **①`assist` を「HOLD だけ採用」の意味に変える ②英語だけ `assist`
      ③model を `jev-1.13.0` に固定**（`assist` 以上では alias を拒否します）。
      **読み上げの開始は今より遅くなります。** そのうえで進めるかをご指示ください。

### B-8e. 相づちを読まない設定の確認（v1.49.12・**既定はOFF**）

**設定を触らなければ挙動は変わりません。** 試す場合は ⚙→音声→「相づちの読み上げ」で
**「つなぎ語だけ読まない」**を選んでください。

- [ ] **落ちるものと落ちないものを確かめる。** 「えー」「あのー」「うーん」「Hmm」は落ち、
      **「はい」「うん」「そう」「なるほど」「Yes」「okay」は落ちません。** これらは単独で
      質問の答えになるので、既定の一覧に**わざと入れていません**（落とすと意味が反転し、
      しかも聞き手には落ちたことが分かりません）。落としたいなら設定の
      「**読まない語の一覧**」にご自分で書いてください。空に戻せば既定に戻ります。

- [ ] **落ちても文字が残ることを確かめる。** 会話カード・議事録・CSV・診断ログには
      **必ず残ります。** カードの **▶** を押せば**必ず読みます**。

- [ ] **落とした数を見る。** 診断の「**相づちの読み上げ**」の行に
      `つなぎ語だけ読まない ／ このセッションで落とした数 7 ／ 一覧は既定` のように出ます。
      **落とした音声は聞き手には存在しないので、この数字が唯一の手がかりです。**
      診断ログでは `tts skip {"why":"相づち（一覧の語）",…}` と
      `segment backchannel-skip` が出ます。

- [ ] **判断が要るところ。** 相づちが落ちると**そのタイミングも失われます。** 「読み上げが
      早く追いつく」ことと「相づちが聞こえる」ことのどちらを採るかは、実際に聞いた
      ご判断です。**合わないと感じたら「全部読む」に戻してください**（それが既定です）。

### B-8f. 設定欄の並び替えと、見つかった版ずれ（v1.49.13）

**先にお詫びです。** 拡張の中のページ（`app.html`）に、**v1.49.11 の「Aivis 60秒に10回を
超えたとき」と v1.49.12 の「相づちの読み上げ」が入っていませんでした。** `index.html`
にだけ足していたためです。設定の仕組みは要素が無ければ黙って飛ばすので、エラーも出ずに
**拡張のページからだけ触れない設定**になっていました。GitHub Pages 版をお使いなので実害は
ありませんでしたが、直して**再発防止の検査**（`test-app-html-sync`）を足しました。

- [ ] **アドオンのポップアップの並び。** 上から「このタブに字幕表示／Web会議マイク音声」→
      字幕の操作8個（ウィンドウ外に字幕・テキスト入力・タブ内字幕を隠す・字幕を消去・
      HTML本体を開く・オーバーレイ表示設定・位置をリセット・字幕操作ログを保存）→
      **一番下に**「HTML本体のURL」と「判断層（Jev）の接続先」。

- [ ] **音声タブの並び。** 「Web会議への音声送出」が**読み上げの出力先デバイスの直下**に、
      「マイク感度」が**「話し方解析の状態」の直下**に移っていること。

- [ ] **読み上げの出力先に「🎧 Web会議マイク音声（アドオン）」が出ること。**
      アドオン未接続なら**（未接続）と出て選べません**。アドオンの「Web会議マイク音声」で
      接続すると、**この欄が自動でそこへ変わります**。デバイスを選び直すと会議への送出が
      止まります。**会議へのトラックは1本なので、自分向け・相手向けで共通の入り切りです。**
      選んでもデバイス指定は消えず、会議を切ると元の出力先に戻ります。

- [ ] **判断層のパネルの文字が小さくなっていること**、「判断の経路」の説明が
      **アドオン経由を選ぶ手順**になっていること、**経路の一覧に `Local` が出ないこと**
      （学習済みの重みを入れる画面がまだ無く、選んでも Rules のまま動く設定でした）。

### B-8g. 設定UIの文字をそろえました（v1.49.14）

**ご指摘のとおり、同じ画面の中で1.4倍の差がついていました。** 「Web会議への音声送出」と
「発話交代の判断層」は**ブラウザ既定の16px**で出ていました。JSから注入するパネルは素の
`<p>` と `<label>` を使うため、クラスが無いとそうなります。

**その場しのぎの数値を足すのをやめ、`設定UI設計指針.md` に段を決めました。**

|段|px|使うところ|
|---|---|---|
|本文|**11.5**|読む文字すべて|
|操作|**12.5**|折りたたみの見出し・ボタン・タブ|
|入力|**16**|`select` / `input` / `textarea`（iOSの自動ズーム防止）|

- [ ] **文字がそろっていること。** ⚙ の各タブを開いて、説明文の大きさが同じに見えること。
      実ブラウザでの実測では、ドロワーの折りたたみ見出しは **12.5px が14個・説明ピルが
      10.5px で9個のみ**、説明文は **64個すべて 11.5px** です。

- [ ] **長い説明がたたまれていること。** 判断層のパネルで「この機能について」
      「経路の選び方と、つなぎ方」「このキーについて」「キーの扱い」
      「原文を記録するときの注意」が**押すと開く形**になっていること。
      **見出しは自動でたたまれる説明と同じピル**です。

- [ ] **APIキーの状態は、たたまずに見えていること（v1.49.15）。** キー欄のすぐ下に
      「**いまアドオンを経由できません。ポップアップから接続してください。**」が
      **出たままになる**こと。仕様の説明（64k context・alias拒否・日本語の注意）だけが
      「このキーについて」の中です。**接続できていないことを隠すと、キーを入れても
      動かない理由が画面から消えます。** 経路を Rules にすると、この折りたたみごと
      消えて「この経路は外部へ送らないので、キーは要りません。」だけが残ります。

- [ ] **文字が小さすぎないか。** 11.5px はドロワーの大多数が既に使っていた値なので、
      今回は外れ値をそろえただけです。**読みにくければ教えてください。**
      変えるのは `設定UI設計指針.md` の表1か所で、CSSは検査が追随を強制します。

### B-8h. assist を「待つ判断だけ」に変え、読み上げ開始にも判断層を使えるようにしました（v1.49.16）

**どちらも既定 OFF のままです**（`turnDecisionMode=off`）。試すときだけ効きます。

#### ① `assist` の意味が変わりました

**これまで `assist` と `active` はコード上まったく同じ**でした。`assist` を選ぶと
実質 `active`（Jev が切る位置を決める）になっていました。

|選択|いまの動き|
|---|---|
|`assist`|**待つ判断だけ採用。切る位置は Rules のまま**|
|`active`|切る位置も Jev が決める|

根拠は実測です。**Jev は一度も「もっと早く切れ」と言っていません**（英語24件・日本語25件
とも0回）。価値は待つ側に全部あるので、そこだけ採ります。

#### ② 読み上げ開始にも判断層（`safe_to_speak`）

毎回聞いて毎回捨てていた答えをつなぎました。**APIの費用は増えません。**

- [ ] **`assist` を試す。** ⚙→音声→発話交代の判断層 で
      **判断モード=assist・英語=assist・日本語は shadow のまま**。
      **model を `jev-1.13.0` に固定**してください（`assist` 以上では alias を拒否します）。
      **翻訳ONの素材**でお願いします。翻訳OFFでは分割が音声に出ないので差が見えません。

- [ ] **切り方が変わったか。** 診断の「判断の内訳」で `確定 provider` が**0のまま**で、
      それでも**1文が3つに割れる現象が減っている**のが期待する形です
      （位置は Rules、待ちだけ Jev、という意味）。ログには
      `segment commit` の前に `boundaryWaiting: provider-hold` が出ます。

- [ ] **読み上げの開始が遅くなっていないか。** これは**遅くなる変更です**。
      HOLD 1回は「次の候補まで待つ」意味です。**許容できなければ `shadow` に戻してください。**

- [ ] **床を試す場合（②）。** 「話者との重なり」を**「自動」か「避ける」**にしてください。
      **「許容」のあいだは働きません**（同時通訳は重ねて読むのが普通で、許容はその明示的な
      選択だからです）。効いたときは診断ログに
      `segment floor-hold {"why":"safe-to-speak","safeToSpeak":0.12,...}` が出ます。
      保持は**答えの鮮度2秒**と**読み上げ開始の待ち上限**の短いほうで頭打ちです。

- [ ] **判断が要るところ。** 床の2つの定数（**鮮度2秒・境目0.25**）は**仮値**です。
      `safe_to_speak` は noul なので 0.5 が「判断がつかない」で、境目はその下に置いて
      あります。**保持が多すぎる／少なすぎると感じたら教えてください。**

### B-8i. 原文の大きさと、`active` が動いていなかった件（v1.49.18）

#### ① 原文サイズを比で決められるようにしました

⚙ → 表示・記録 → 表示 → **原文サイズ**（40〜100%・5%刻み・**既定 60%**）。

いままでは面ごとに直書きで、設定はありませんでした — 会話バブル **50%**、タブ内字幕
**58%**、別ウィンドウ字幕 **65%**。3面ともこの1つの数字を見ます。**比なので文字サイズを
変えても関係は変わりません。**

- [ ] **本体の会話バブルで、原文が大きくなること。** 40〜100% を動かして確かめてください。
- [ ] **別ウィンドウ字幕（PiP）でも同じ比になること。**
- [ ] **タブ内字幕でも同じ比になること。** こちらは拡張が profile を送るので、
      **アドオンを 1.4.29 に更新してから**確認してください。
- [ ] 併記のチェックを外したときは、従来どおり原文が消えること。

#### ② `active` の会議ログは、Jev が一度も動いていませんでした

いただいたログの結論です。**これは Jev の評価になっていません。**

```
判断層の往復 | 送信 13 / 正常応答 0 / 適用 0
判断の内訳   | 確定 rules 85 / provider 0
turn-circuit-open {"reason":"active では model に固定versionを指定してください（いまは jev-latest…）"}
```

`assist` 以上で alias を拒否するのは仕様どおりですが、**経路の既定 model が `jev-latest`**
だったため、model 欄を空のままにすると**毎回こちら側で拒否して Rules へ落ちる**状態でした。
訂正率 0.0% も「Jev が良かった」ではなく「Jev が居なかった」結果です。

|直したこと|
|---|
|経路の既定 model を **`jev-1.13.0`** に変更（空欄のままで `active` が動きます）|
|拒否のメッセージを「**assist／active では**」に修正（assist でも拒否されます）|
|自分で alias を入れているときは、状態行に **⚠ …送信は毎回拒否されます** と出す|

- [ ] **もう一度 `active` を試してください。** model 欄は**空のままで構いません**。
      状態行の **`往復 送信N/応答N`** で、応答が 0 でないことを最初に確認してください。
      ここが 0 のままなら、その会議は判断層を評価できていません。

#### ③ 読み上げが始まらなかった件

`tts_generating` のまま `audio cancelled` になったカードが1枚ありました。**原因は必ず
`audio-cancel {why}` に残ります**が、その行は保持件数（800件）の外に落ちていました。

|直したこと|
|---|
|行動ログの保持を **800 → 3000件**（7分の会議で最初の6分が消えていました）|
|`audio-start` が**上限を無視して**書き込んでいたのを `dlog()` 経由に|
|診断のカード行に**理由**を出す（`audio cancelled（speech-stopped）` のように）|

- [ ] **次に同じことが起きたら、診断のカード行の括弧を見てください。**
      `speech-stopped`（読み上げOFF・設定変更・別の文を再生・認識停止など）、`echo`、
      `source-or-translation-updated`、`translation-error`、`floor-expired`、
      `backchannel`、`conference-off` のどれかが出ます。

### B-8j. `active` は動いていました。ただし答えが毎回「一手遅れ」でした（v1.49.19）

#### ① 何が起きていたか

接続は**完全に正常**です。にもかかわらず採用は0回でした。

```
判断層の往復 | 送信 51 / 正常応答 51 / 解析失敗 0 / 到達不能 0 / 適用 0
判断の内訳   | 確定 rules 77 / provider 0
```

理由はログの時刻に出ています。

```
[+246.163s] turn-decision-request  candidates 3
[+246.163s] commit seq=1 / seq=2 / seq=3      ← 同じミリ秒で Rules が3つに切った
[+246.355s] turn-decision-result   choice C_STABLE_159   ← 192ms後「切らず159文字で1つ」
```

質問は **commit と同じ tick** で飛び、答えは 174〜483ms（中央値およそ205ms）で返ります。
録音分割REST（`gpt-4o-mini-transcribe`）では**確定した文が一度に届く**ので、
文字が伸びていく途中の局面がありません。**この経路では active は構造的に空回りします。**

#### ② 直したこと — 有界の待ち（INV-10）

⚙ → 音声 → 発話交代の判断層 → **確定を待つ上限 ms（0＝待たない）**（既定 **300ms**）

|規則|
|---|
|待つのは**いま投げた、この文の答えだけ**（別の質問の往復は待たない）|
|上限を過ぎたら **Rules が確定する**（答えが来なくても止まらない）|
|**`off` と `shadow` は一切待たない**（INV-08 はそのまま）|
|`0` にすれば v1.49.18 と同じ挙動に戻る|

- [ ] **`active` でもう一度お願いします。** 状態行の **`往復 送信N/応答N/適用N`** で、
      **適用が 0 でなくなる**ことが今回の合否です。
- [ ] **読み上げ開始が遅くなっていないか。** 理屈の上では1区切りあたり最大0.3秒増えます。
      実測では commit から再生まで元から 1.6〜22.2秒あるので埋もれるはずですが、
      **体感で分かるようなら 0 にしてください**（それで従来どおりに戻ります）。
- [ ] **切り方が変わったか。** 診断の「判断の内訳」で `確定 provider` が増え、
      1枚のカードが2〜5個に割れる現象が減っていれば期待どおりです。

#### ③ Jev が実際に言っていたこと（51件）

|Jev の答え|件数|
|---|---:|
|Rules に同意|32|
|**もっと後ろで切れ**（割らず1つに）|14|
|**HOLD（待て）**|5|
|もっと前で切れ|**0**|

**3回のログ・約100件を通じて「もっと早く切れ」は一度も出ていません。**
「後ろで切れ」14件が「切り方が短すぎる」の正体です（例：`45/33/96/39/107` の5分割に対し
「320文字で1つ」、`72/43/6/84` の4分割に対し「205文字で1つ」）。

#### ④ まだ直していないこと — カードをまたぐ結合

HOLD 5件は**カードをまたぐ**同じ問題です。30秒の録音区切りが文の途中に落ちるため

> `…on this continent,` ／ `I can report to you with pride that...` ／ `America is back...`

が3枚のカードになり、日本語も3文に割れて読まれます（Jev は前2つを HOLD 0.9 と判定）。
結合には**次の録音区切りを待つ＝数秒**が要るので、今回は手を付けていません。

#### ⑤ 取り消しの理由（v1.49.18 の穴）

最後のカードが理由なしの `audio cancelled` になっていました。実際は
`+601.941s audio-cancel {"why":"認識停止"}` ＝ **停止ボタン**です。`segCancelAudio` が
`skipReason` を残していなかったので、v1.49.18 で入れた「カード行に理由を併記」が
**いちばん多い経路で効いていませんでした**。直しました。

#### ⑥ 判断層とは無関係の所見

- **読み上げが最大30.8秒遅れています**（`debtEstimateSeconds`）。原因は 30秒の録音区切りで、
  1枚のカードが325〜429文字になり、話速2.0でも追いつきません。**Jevでは直りません。**
- 最初の10枚はポルトガル語（ブラジル大統領）を `en` として認識・翻訳しています。
  訳自体は成立していますが、席Bの言語は固定なので原文の言語名は英語のままです。

### B-8k. `active` は採用されました。同時に3つの穴が見えました（v1.49.20）

#### ① 待ちは効きました

トランプ演説の2記録で、**採用が0でなくなりました。**

|記録|STT経路|時間|判断の内訳|
|---|---|---:|---|
|1本目|録音分割REST（6秒上限）|187秒|確定 rules 23 / **provider 1**|
|2本目|`gpt-live-transcribe`|406秒|確定 rules 54 / **provider 6**|

1本目で Jev が決めた1件は
`It's my great honor to welcome the world to America as we celebrate 250 years.`
です。**Realtime のほうが採用率が約6倍高い**のは、`live-segment-delta` が
200〜1200msごとに届き、文字が伸びていく途中の局面が本当に存在するからです。
**判断層を試すなら Realtime 経路をお使いください。**

- [ ] **今回も `active` でお願いします。** 見るところは3つです。
      `確定 provider` が増えているか、`往復` の**送信**が数百件にならないか、
      **打切り**（新しい表示）が増えていないか。

#### ② 穴1 — 「待て」に期限が無く、カードが停止まで放置されていました

両方の記録に、**112秒と83秒のあいだ確定されず、最後に読み上げごと消えたカード**が
あります（`stabilityMs 112557` / `83967`、いずれも `audio cancelled（speech-stopped）`）。

判断層の答えは `(文, 版)` を鍵に保存し、版は**新しいテキストが来たときだけ**進みます。
認識が止まると版も止まるので、そこで最後に受け取った「まだ切るな」を80ms周期で
**読み直し続けます**。待つ理由は永遠に「いま来たばかり」で、確定も翻訳も読み上げも
起きません。INV-09 は読み上げ開始に、INV-10 は答えの到着に上限を置きましたが、
**返ってきた「待て」そのものには上限がありませんでした。**

⚙ → 音声 → 発話交代の判断層 → **「待て」に従う上限 ms（0＝従わない）**（既定 **2000ms**）

|規則|
|---|
|`HOLD`・言い直し待ち・位置確認のどれでも、上限を過ぎたら **Rules が確定する**|
|時計は**確定済みの部分ごと**。話が続いて文が伸びても止まらない|
|打ち切った部分は、確定するまで**静かに** Rules へ通す（ログは各1行）|
|`0` で「待て」を最初から採らない。`off`／`shadow` は無関係|

- [ ] **カードが放置されないこと。** 通訳を終えて停止したとき、
      **`audio cancelled（speech-stopped）` のまま読まれなかったカードが無い**ことを
      見てください。ここが今回の主眼です。

#### ③ 穴2 — 内容の同じ往復が数百回（そのまま費用です）

```
1本目  送信 433 件 ／ 187秒   （revision:1・37文字・候補3 の同じ往復が大半）
2本目  送信 1251 件 ／ 406秒  （revision:85・78文字 の同じ往復が大半）
```

再送の抑止は「テキスト・候補・無音」が同じなら300ms止める作りでしたが、
**無音を100ms刻みで数えていた**ため、テキストが凍っても無音が伸びるかぎり
毎回「新しい状態」として通っていました。穴1で止まったカードが、そのあいだ
ずっと聞き直していたということです。1回の通訳で **0.6M／1.7M入力トークン**相当です。

⚙ → 音声 → 発話交代の判断層 → **同じ版を聞き直す上限回数**（既定 **3**）

- [ ] **送信数を見てください。** 数十件の桁なら直っています。
      数百件のままなら、まだ別の経路で往復しています。

#### ④ 穴3 — 「適用」の数が意味を持っていませんでした

送信 433 に対し適用 2450、送信 1251 に対し適用 3487。**保持している80msの tick ごとに
数えていた**ためです（2450 × 80ms ≒ 196秒で、記録の全長187秒とほぼ同じ ――
「ほぼ常にどこかが保持されていた」という意味でした）。

- **適用は答え1件につき1回**に直しました。送信数と並べて読める数になります。
- tick の数は **保持tick**、打ち切った回数は **打切り** として別に出します。

```
判断層の往復 | 送信 120 / 正常応答 118 / 解析失敗 0 / 古くて破棄 2 / 到達不能 0 / 適用 31　保持tick 240 / 打切り 3
```

#### ⑤ 今回直していない所見（記録に見えています）

- **Realtime で `Prosody取得状態 Pitch=未検出`。** WebRTC 経路ではマイク波形が解析器に
  流れておらず、音響特徴を渡す設定を入れてもピッチは空で送られます。
  2本目の採用6件はすべて**音響なし**で出ています。
- **`tts-wait-end {"reason":"sentence-boundary","waitMs":15043}`**（2本目 e10）。
  文末を待って15秒。床の上限とは別経路で、まだ縛っていません。
- **カードが大きい**（2本目は最大501文字・8部分、重なり54〜69%）。カードをまたぐ結合は
  次のSTT塊を待つ必要があるため、今回も手を付けていません。

### B-8l. Realtime の音響と、Google の読み上げ（v1.49.21）

#### ① `Pitch=未検出` は直りました

v1.49.19 の `gpt-live-transcribe` 記録で、判断層が決めた6件はすべて**音響なし**でした。
原因は2つ重なっていました。

1. **判断層が共有マイクの解析器しか見ていなかった。** 解析器は3系統（共有マイク・
   録音分割RESTのエンジン・Realtime系のエンジン）ありますが、読んでいたのは
   共有マイクだけです。**マイクを使わない経路（タブ音声・VB-CABLE・画面共有）では
   音響がいつも空**でした。演説を訳す使い方はまさにこれです。
2. **`gpt-live-transcribe` のエンジンには解析器が無かった。** 境界判定用の512点は
   ありましたが、512点だと YIN の探索下限が48kHzで**約189Hz**になり、
   男性の話声（85〜180Hz）が丸ごと範囲外に落ちます。

直したのは、読む解析器を**席で決める**ようにしたことと、`gpt-live-transcribe` に
**解析専用の2048点ノード**を足したことです。境界判定のRMS窓は変えていません。

- [ ] **診断の「Prosody取得状態」を見てください。** 末尾に `解析器` が付きます。

```
Prosody取得状態 | Pitch=可 / Energy=可 / Timing=可 / 解析器 live:audio
```

      `Pitch=可` になっていれば直っています。`解析器 無し` と出たら、そのときは
      音響なしで判断しています。
- [ ] **字幕カードの音響表示。** タブ音声でもカードに音響が出るようになりました。

#### ② Google Cloud TTS を追加しました

⚙ → 音声 → 読み上げ → **Google Cloud TTS（Chirp 3: HD / Gemini-TTS）**

**拡張機能も中継サーバーも要りません。** このAPIはブラウザからの直接呼び出しを
許可しています（実測で確認しました）。判断層の `api.typesafe.ai` とは違います。

|系統|声の名前|向き|
|---|---|---|
|**Chirp 3: HD**（既定）|`ja-JP-Chirp3-HD-Aoede` のように自動で組み立て|低遅延。同時通訳向き|
|**Gemini-TTS**|`Kore` のような素の名前＋話し方を文章で指示|表現重視。1発話の待ちは伸びる|
|名前を直接指定|入力した名前そのまま|Neural2 / Studio / WaveNet など|

- [ ] **まず「🔍 確認」を押してください。** キーの確認と声の一覧取得は同じ処理です。
      件数が返れば、読み上げと同じ経路がまるごと生きています。
      **この環境にGoogleのキーが無いため、音が鳴るところは未確認です。**
      最初の1回はここで確かめてから読み上げてください。
- [ ] **Chirp 3: HD で両席の声を変えてみてください。** 同じ名前を選んでおけば
      **どの言語でも同じ声**で読みます。A席とB席で別の名前にすると、
      どちらの発言か耳で分かります。
- [ ] **話速。** Chirp 3: HD と Gemini-TTS は**API側の話速指定に対応していません**
      （Chirp 3: HD は既定値を送っても 400 が返ります）。この2つでは話速を
      ピッチ保持再生で出します。診断の「Google 話速処理」に
      「実効◯倍 / API◯倍 / ピッチ保持再生◯倍」が出ます。
- [ ] **Gemini-TTS の「話し方の指示」。** 例：`落ち着いた通訳者の口調で、淡々と読んでください`。
      速さもここに書けますが、数値で確実に効くのは上の話速です。

#### ③ 実装上の注意（つまずきやすい所）

- キーは <b>Google Cloud →「APIとサービス」→「認証情報」</b>で作り、同じプロジェクトで
  **Cloud Text-to-Speech API を有効化**してください。キーの制限はこのAPIだけに絞るのが安全です。
- **キーが無効なときの応答は 401 でも 403 でもなく 400** です（実測）。
  声の名前の問題と区別できるよう、メッセージを分けています。
- 中国語は `cmn-CN` / `cmn-TW`、アラビア語は Chirp 3: HD が `ar-XA`、
  Gemini-TTS が `ar-001` です。既定のBCP-47をそのまま送ると 400 になるため、
  こちらで置き換えています。

#### ④ 今回確かめていないこと

- **合成そのもの。** キーが無いため、CORS・エンドポイント・リクエストのスキーマ・
  無効キーの応答は実際のGoogleに当てて確認しましたが、**音は出していません**。
- **Gemini-TTS が本当に `speakingRate` を拒むか。** 公式に記述があるのは
  Chirp 3: HD だけです。通るなら送るほうが正確です。
- **Realtime の音響が判断を変えるか。** 解析器は繋がりましたが、
  音響ありの `active` で切り方がどう変わるかは次の記録を見るまで分かりません。

### B-8m. Gemini 3.8 と、モデル名を表から外したこと（v1.49.22）

v1.4.32 で入れた Google Cloud TTS のモデル選択は**プルダウン**で、中身は Google の
公式サンプルにあった4つ（`gemini-2.5-flash-tts` / `gemini-3.1-flash-tts-preview` /
`gemini-2.5-pro-tts` / `gemini-2.5-flash-lite-preview-tts`）でした。
**その2日後に Gemini 3.8 Flash TTS と Gemini 3.8 Flash-Lite TTS が出ています。**

表で持っているかぎり、Google が新しいモデルを出すたびにこちらの版を上げないと
使えません。APIは自由文字列を受ける欄なので、作りのほうが間違っていました。

#### ① 直したこと

⚙ → 音声 → 読み上げ → Google Cloud TTS →（系統を Gemini-TTS にすると出る）**モデル**

- **入力欄になりました。** 候補は出ますが、選ばずに打ち込めます。
  **新しいモデルが出たら、このアプリを更新しなくてもその名前を書けば使えます。**
- 既定を **`gemini-3.8-flash-lite-tts`** にしました（読み上げ用途向けの高速・低コスト版で、
  `gemini-3.1-flash-tts-preview` の置き換えにあたります）。表現を優先するなら
  `gemini-3.8-flash-tts` です。
- **声の設計・声の複製**で作った声は、その識別子を「**声の名前を直接入力**」へ貼れば使えます。

- [ ] **モデル欄に `gemini-3.8-flash-lite-tts` が入っていることを確認してください。**
      診断の「読み上げモデル」にもそのまま出ます。
- [ ] **打ち込みも試してください。** 候補に無い名前を書いても、そのまま送ります。

#### ② 変えていないこと

リクエストの形（`voice.modelName` に書いた名前をそのまま載せる）、音声形式の
**MP3** 指定、**Gemini-TTS には `speakingRate` を送らない**方針は v1.4.32 のままです。

#### ③ 確かめていないこと（v1.4.32 から変わらず）

**合成そのものは未確認です。** この環境に Google のキーがありません。
モデル名が書いたとおりに送られることはブラウザで確認しましたが、
`gemini-3.8-flash-lite-tts` が実際に応答を返すかは試していません。
**最初の1回は「🔍 確認」でキーの疎通を見てから**読み上げてください。

### B-8n. Teams で声が届かなかった件・遅延・ワンタッチ（v1.49.23）

#### ① 止まっていたのは「会議マイク送出」でした

`gpt-live-transcribe` の時間帯に自動で鳴った読み上げは2件だけで、
**その2件とも `webConferenceMicEnabled:false` の瞬間**でした。
`gpt-4o-mini-transcribe` に切り替えたあとの4件はすべて `true` です。
**モデルの違いではありません。**

|時刻|できごと|
|---|---|
|+318.9s / +343.9s|ボタン → **拒否**「会議送出にはAivis・OpenAI等の音声生成TTSを選んでください」（読み上げが `off` のまま）|
|+350.6s|読み上げを Aivis に変更|
|+363.7s|有効化成功 → **+371.1s 停止**（理由 `error`・本文空）|
|+428.3s|有効化成功 → **+466.8s 停止**（`relay-timeout`）|
|+473.5s|有効化成功 → +491.8s セッション停止|

**「カードを先頭から再生」は会議へ流れません。** 確認で押した2回はどちらも
`conference-route-rejected`（`playbackIntent:"manual-replay"`）です。手で鳴らした音は
手元だけで鳴ります。**相手に聞こえるかの確認は、実際に喋って自動再生させてください。**

- [ ] 次に試すときは、**先に読み上げを Aivis 等にしてから**送出ボタンを押してください。
- [ ] 送出が途中で切れていないか、アドオンの「字幕操作ログを保存」で
      `webConferenceMicEnabled` の行を見てください。

#### ② gpt-live の遅れは、まだ切り分けできません

同じログで部分結果の到着間隔が `7131 / 11958 / 11787 / 42579 / 65609 ms` でした。
受け取ってから画面に出るまでは 2〜13ms なので**こちら側ではありません**が、
**音声がこちらから出ていたかを示す記録がこの経路だけ無く**、特定できませんでした。

`live-audio-transport` を5秒ごとに記録するようにしました。

- [ ] 次に gpt-live で同じことが起きたら、ログの **`bytesSinceLast`** を見てください。
      伸びていれば音は出ています（相手側の遅れ）。0のままならこちらが出せていません。

#### ③ 遅延 — 縮められるのは「無音待ち」だけでした

|区間|実測|
|---|---|
|話し終わり → 送信（**無音待ち**）|**0.9秒**（固定値でした）|
|送信 → 文字が返る|0.69〜**2.33**秒（相手側の時間）|
|確定 → 声の先頭|0.27〜0.39秒|
|**合計**|**1.95 / 1.95 / 3.6 秒**|

⚙ → 音声 → 音声認識 → **話し終わりを待つ無音 ms**（既定 **900**、200〜2000）

- [ ] **500ms で試してみてください**（0.4秒短くなります）。自分の声をそのまま届ける用途なら
      300ms も可です（0.6秒短縮）。
- [ ] 代わりに**息継ぎで文が割れます**。割れ方が気になるようなら戻してください。
      認識の回数（＝料金）も増えます。
- `gpt-live-transcribe` はこの値を使いません。**本来そちらのほうが速い経路**なので、
  ②の切り分けを先に足しました。

#### ④ 「Web会議マイク音声」がワンタッチになりました

押すと、足りない手順だけを順に実行します ――
**このタブに字幕表示 → HTML本体を開く → 翻訳開始 → 会議マイクへ送出**。

- 済んでいる手順は飛ばします。**送出中に押すと、従来どおり送出だけを止めます。**
- **HTML本体は前面に出しません**（ポップアップは焦点を失うと閉じ、残りが中断されるため）。
  開いて、名乗り出るのを最大12秒待ちます。
- 途中で止まったら **どの手順で何が起きたか**を出します。
- **前の手順が失敗したら後ろは実行しません**（認識していないのに送出だけONにしない）。
- プリセットや入力元の構成には触れません。

- [ ] **Teams のタブで1回押すだけで通るか**を見てください。ここが今回の主眼です。
- [ ] **HTML本体も一緒に更新してください。** 古いままだと
      「HTML本体をv1.49.23以降に更新してください」と出ます。
- [ ] 新しく開いた本体のタブで、**マイクが取れて音量メーターが振れるか**。
      許可済みのサイトなので操作なしで始まるはずですが、実機では未確認です。

### B-8o. キーボード入力が読み上げられなかった件（v1.49.24）

**翻訳の停止・実行とは無関係でした。** 停止中の2件も実行中の2件も、同じ理由で外れています。
読み上げ対象が「**相手(B)の発言のみ**」のところへ、打ち込んだ日本語が
**自分(A)の発言**（言語A=ja の席）として入っていました。声で話したときと同じ規則です。

- 理由を分けて残すようにしました（`tts-target-B2A` など。以前は `not-selected-or-empty` の1語）。
- **入力した文が対象外で外れたときは、1回目から画面に案内します。**
- **入力した文には判断層（Jev）をかけません。** 以前は1件ごとに3往復し、2秒止めてから確定していました。

- [ ] 読み上げ対象「相手(B)の発言のみ」のまま日本語を入力 → **案内が1回出る**こと。
- [ ] 読み上げ対象を「**両方**」にして入力 → **すぐ読み上げられる**こと（2秒待たないこと）。
- [ ] または入力欄の話者を **B** にして送る → 読み上げられること。

### B-8p. Web Speech（日本語）に句読点が付かなかった件（v1.49.25）

Chrome の Web Speech は日本語に句読点を付けず、**息継ぎの位置に空白を入れて**返します。
これまでは空白のまま流していたので、文末が一つも無く、**120字の長さ打切りまで切れません**
でした（カード1枚110〜120字、最後の一文にだけ「。」）。

- 空白を、**直前の語尾**で「。」「？」「、」に置き換えます（です・ます・ですよね → 「。」、
  ですか・ませんか → 「？」、それ以外 → 「、」）。途中結果にもかけるので、話している最中に
  文末で切れて翻訳が先に進みます。
- 「〜みる」「〜違う」のような言い切りは、文末か文の途中か語尾だけでは決められないので「、」にとどめます。
- 「Google の」のように英数字に接する空白は、単語の区切りなので残します。
- 判断層（Jev）は、**テキストが0.2秒止まってから**聞くようにしました（⚙→判断層「話が止まってから聞くまで」）。
  途中結果は0.1秒ごとに版が進むため、話している最中の質問は必ず捨てられていました（約130秒で送信525件・採用0件）。

- [ ] Web Speech（日本語）で話す → 字幕に「。」「、」が入ること。
- [ ] 「〜ですよね」で区切って話し続ける → **話し終わる前に**前の文が確定・翻訳されること。
- [ ] Jev を active にして同じように話す → 診断の送信数が話している秒数×4 より**ずっと少ない**こと。

### B-8q. Web Speech の途中結果を1枚に／息継ぎを判断層の候補に（v1.49.26・拡張 1.4.37）

**拡張と HTML 本体の両方を更新**してください。

- [ ] HTML 本体を**ブックマークから直接**開き、判断層「Jev — アドオン経由」で開始 → 診断の
  「判断層の往復」で**到達不能が増えない**こと（字幕表示は押さない）。
- [ ] Web Speech で話し続ける動画を認識 → 同じ発話が**2枚のカードに割れない**こと。
- [ ] 同じ動画で、確定した部分の末尾に「。」だけでなく「**？**」も付くこと
  （「〜なの」「〜っけ」などの問いかけ）。付いた部分は読み上げの語尾が上がること。
- [ ] 診断の「逐次読み上げの部分別状態」で、`provider-C_PAUSE_S` / `provider-C_PAUSE_Q` の確定が
  出ること。112字前後の `semantic-length-fallback` が**減る**こと。
- [ ] 診断の「判断層の往復」の送信数を記録する（1分あたり何回か）。費用の見積もりに使います。

### B-8r. Chrome の60秒確定で文の途中からカードが割れない（v1.49.27）

Chrome は話し続けていても約60秒で結果を確定させます。文の途中でもカードを閉じないようにしました。

- [ ] Web Speech で1分以上話し続ける動画を認識 → カードが約60秒ごとに**文の途中で割れない**こと
  （「…全ての操作を。」のような、文の途中に付いた「。」が出ないこと）。
- [ ] 話をはっきり止めたときは、約1.5秒以内にカードが閉じること。
- [ ] 「〜です」で確定した部分のあと、次の文を待たずに読み上げが始まること
  （診断で `tts-wait` の `waitMs` が数秒に伸びていないこと）。

### B-8s. gpt-live のカードを判断層で早く閉じる（v1.49.28・既定は記録だけ）

- [ ] 英語で gpt-live、判断層 active、「gpt-live のカードを閉じる判断」＝記録だけ で数分話す。
- [ ] 診断の「gpt-live の区切り（判断層）」を記録する（従えば早く閉じた回数・平均何ms早いか・その後に話が続いた回数）。
- [ ] 話が続いた回数がほぼ0なら「判断層に従って早く閉じる」にして同じように話し、
  「。」で終わらない発話の最後が、止めてから0.5秒前後で読み上げ始まること。
- [ ] 判断層が HOLD と答えていても、確定した最後の部分がすぐ読み上げに回ること
  （診断で `turn-decision-hold-expired` が確定済みの本文に出ないこと）。

### B-8t. 読み上げの待ちを短く／アドオンのタブ音声と Web Speech（v1.49.29・拡張 1.4.40）

- [ ] Aivis・ストリーミング再生 ON・「次の部分の先読み」＝先読みする で、1文ずつ区切って話し続ける。
  部分の切り替わりの無音が v1.49.28 より短いこと、声が重ならないこと。
- [ ] 診断の「Aivis の先読み」を記録する（先読み・使用・捨てた回数と理由、何ms前に頼んだか）。
  「捨てた」が多い、または「Aivis 60秒10回を超えたとき」の回数が増えて困るなら「先読みしない」に戻す。
- [ ] 句点の来ない話し方（「〜して、〜して、」と続ける）で、確定した部分の読み上げが、
  止まらずに約1.2秒以内に始まること（診断ログの `tts-wait-capped`）。
- [ ] 判断層 active で話し、「判断層の往復」の「句点で待たず」が増えること。
- [ ] アドオンの字幕表示＋Web Speech で開始する。
  - 認識できれば、診断ログに `track-heard` が出る。`track-relay` の後に出ていれば、中継で直ったということ。
  - 認識できなければ、数秒で止まり「アドオンのタブ音声では…」の案内が出ること（`track-ERROR network` が延々と続かないこと）。

### B-8u. 音は届いているのに Web Speech が文字を返さない（v1.49.30・拡張 1.4.41）

- [ ] 相手(B)の言語を、流れている音声の言語に合わせてから、画面共有＋Web Speech で数分流す。
- [ ] 文字が止まったら、診断ログで `track-stall` と、その後の `track-restart`（`why:"stall"`）→ `track-start` → 結果、の順になっていること。
- [ ] 音楽だけの場面で、`track-stall` の `waitedMs` が 10000→20000→40000→60000 と伸びること。
- [ ] わざと言語を違えて流すと、2回目の始め直しで言語を確かめる案内が1回だけ出ること。
- [ ] 診断ログに `app ERROR` が出ていないこと（出ていればその行を添えて報告）。

### B-8v. gpt-4o 系で言語を伝える／遅れているあいだの読み上げ（v1.49.31・拡張 1.4.42）

- [ ] 言語A・B とも日本語、言語切替 AUTO、gpt-4o-mini-transcribe、共有音声で日本語のニュースを数分流す。
  - 診断ログの `4o-file-result` に `"language":"ja"` が出ること。
  - 日本語以外の文字（キリル文字・ハングルなど）のカードが出ないこと。
- [ ] 言語B を英語にして同じように流し、`"language":"auto"` になること。
- [ ] Aivis・Prosody TTS反映 ON・adaptive のまま流し、読み上げが遅れてきたら次を確かめる。
  - 診断ログの `dispatch` に `prosodyMerged:true` と `parts` 2以上が出ること。
  - 「Aivis 60秒10回を超えたとき」の直近60秒の回数が、v1.49.29 の記録（最大25回）より少ないこと。
  - `prosody-map` に `slowHeld` が出るとき、`effectiveRate` が設定の話速より下がっていないこと。

### B-8w. gpt-4o 系で息継ぎのたびにカードが分かれない（v1.49.32・拡張 1.4.43）

- [ ] gpt-4o-mini-transcribe・末尾繰越 ON・無音待ち 450ms で、日本語のニュースを数分流す。
  - 「情報を」「一方で、」のような、文の途中で終わる短いカードがほとんど出ないこと。
  - 文の途中で切れたカードは開いたまま表示され、次の結果が同じカードに続くこと。
  - 診断ログの `4o-tail-held` に `"cut":"silence"` が出ること。
- [ ] 話し手が止まったとき、文の途中で終わっていても、1〜2秒でカードが確定すること。
- [ ] 音の来ないマイクを片方の席に入れたまま始め、診断ログに6秒ごとの `4o-recording-cut`（`reason:"limit"`）と `discard` が並ばないこと。

### B-8x. 翻訳モデル（OpenAI）の初期表示（v1.49.33・拡張 1.4.44）

- [ ] 翻訳プロバイダを OpenAI にし、APIキーを入れて「🔄 更新」を押す。
  - 翻訳モデルの一覧に、`gpt-5.6-luna`・`gpt-5.6-terra`・`gpt-5.6-sol` と、6系の日付なしモデルだけが並ぶこと。
  - 日付付きの版（`…-2026-…` など）が「▼ 他 ◯ 件のモデルを表示」を開くまで出ないこと。
- [ ] 6系のモデルを選んで翻訳し、訳文が出ること。

### B-8y. OpenRouter・Groq の翻訳モデル選びと接続テスト（v1.49.34・拡張 1.4.45）

- [ ] 翻訳プロバイダを OpenRouter にし、APIキーを入れる。
  - 「🔎 選ぶ」を押し、速さ・精度・安さ・人気のそれぞれで3件ほど出ること。行に価格と「応答まで 約◯秒」が出ること。
  - 「全部から選ぶ」で作った会社が件数つきで並び、会社を押すとモデルが出ること。検索と「無料だけ」が効くこと。
  - 「試しに使う」で訳文と時間が出ること。「これにする」で翻訳モデルが変わること。
- [ ] 「APIキーを確認」で、間違ったキーだと失敗すること（前は通っていた）。
- [ ] `deepseek/deepseek-v4.1-flash` で話し、訳が出ること。診断の「OpenRouter」の行に、最後に翻訳した提供元が出ること。
- [ ] 「接続テスト」を押し、4つの結果を記録する。特に「読み上げ」「音声認識」が通るか（通らなければ次の版の ③④ は入れられない）。
- [ ] 翻訳プロバイダを Groq にし、「🔎 選ぶ」の一覧に whisper・orpheus・guard が出ないこと。`qwen/qwen3-32b` で訳に考えた過程が混ざらないこと。

### B-8z. OpenRouter・Groq の読み上げ（v1.49.35・拡張 1.4.46）

- [ ] ⚙→音声 の読み上げを OpenRouter にし、声の一覧が出ること（Gemini 3.8 Flash TTS なら30種）。
- [ ] 「🔎 選ぶ」→「試しに使う」で見本が鳴ること。
- [ ] 会議で読み上げ、声の高さ・速さが自然なこと。診断ログの `openrouter-ok` の `kind`・`rate`・`contentType` を記録する（高すぎる／低すぎるならサンプルレート違い）。
- [ ] 読み上げを Groq にし、英語の訳文が Orpheus の声で、日本語はブラウザ内蔵の声で読まれること。

### B-8za. OpenRouter の音声認識（v1.49.36・拡張 1.4.47）

- [ ] ⚙→音声認識 のサービスを OpenRouter にし、モデル欄に文字起こしモデルが入ること。「🔎 選ぶ」に文字起こしモデルだけが出ること。
- [ ] 話して、会話カードに文字が出ること。診断ログの `openrouter-stt`（送った形式・言語）と、`openrouter-wav-retry` が出たかを記録する。
- [ ] `openai/gpt-4o-mini-transcribe` を選ぶと、「4o系の録音上限」の設定が出ること。
- [ ] 用語集に専門語を入れ、OpenAI の文字起こしモデルで聞き取りが変わるかを見る（効かない可能性がある）。

### B-9. trace が取れたあとの課題（まだ手を付けていません）

公式の jaggedness ページは「数を数えるのは苦手」「数値表現より意味表現のほうが強い」
と明記しています。**いまの設計はここに正面から当たっています。**

- `stablePrefixChars` は文字数そのもの
- 候補IDに `C_FULL_28` のように文字オフセットが入っている
- `silenceMs` `lastDeltaMs` と prosody の各値はすべて生の数値

**それでもまだ直していません。Jev の応答を1件も受け取っていないので、直しても
良くなったか分からないからです。** 前後比較の基準が無いまま表現形を作り替えるのは、
「接続成功を確認する前に周辺実装が先行する」の繰り返しになります。

trace が取れたら、次の順に shadow で前版と比べます。

1. 候補IDを数字を含まない名前にする（対応はコード側が持つ）
2. `silenceMs` `lastDeltaMs` を名前付きバケットにする
3. prosody の生値を「下降／平坦／上昇」のような意味に寄せる

---

## C. 私が測れなかった項目（Phase 0 の残り）

実会議と社内ネットワークが必要で、この環境では実行できませんでした。

- [ ] **話者帰属の実会議精度と遅延。** `次期仕様実装状況.md` が「実会議の精度は未測定」としている項目です。Floor Gate（Phase 3）はこの信号に依存するので、先に測る必要があります。
- [ ] **東京から `api.typesafe.ai` への実測遅延。** p50 が soft deadline 350ms に対してどこに来るかで、Boundary Gate（Phase 4）に意味があるかが決まります。

```
for i in $(seq 20); do curl -o /dev/null -s \
  -w '%{time_connect} %{time_appconnect} %{time_total}\n' \
  https://api.typesafe.ai/v1/models; done
```

- [ ] **体感遅延の内訳分解。** 輸送窓・境界・床のどこが主因かを trace から出します。これが「Jev を入れる前に streaming STT を既定にするだけで足りるか」の判断材料になります。

---

## D. 不合格のときに戻す手順

|層|手段|戻る範囲|
|---|---|---|
|1 設定|⚙→音声 の判断モードを off にする（再読込不要）|判断層の挙動すべて。即時|
|2 版|`index.html` と `extension/app.js` を前版へ差し替え|Phase 1 のリファクタを含むすべて|

第1層で戻らない場合は Phase 1 のリファクタ起因です。`turnDecisionMode` は
そこを無効化できないため、版で戻します。今回の2つの修正は独立コミットなので、
`d5a5684`（adaptive）と `61a9464`（Web Speech）を個別に revert できます。
