# 設定UIの設計指針

対象は **⚙ 設定ドロワー**（`index.html` と `extension/app.html`）と、そこへ **JSから注入するパネル**です。

## なぜこの文書が要るのか

手書きの markup は `.field > label` などの既存クラスに載るので勝手に揃います。ところが
**JSから注入するパネルは素の `<p>` と `<label>` を使う**ため、クラスを付け忘れると
**ブラウザ既定の 16px** で出ます。実際に「Web会議への音声送出」と「発話交代の判断層」が
そうなっていて、隣の区画の 11.5px と並んで**1.4倍の大きさ**で表示されていました。

数値を各所の `#id` へ書き足して合わせると、次に足す人がまた別の値を書きます。
**段を決めて、そこから外れないようにする**のがこの文書の目的です。

---

## 1. 文字サイズは3段だけ

|段|px|使うところ|
|---|---|---|
|**本文**|**11.5**|読む文字すべて — 区画の見出し、小見出し、説明、注意、チェックボックスの行、状態表示|
|**操作**|**12.5**|押すもののうち、フォーム部品ではないもの — 折りたたみの見出し、`.btn`、タブ|
|**入力**|**16**|`select` / `input` / `textarea`|

**入力欄を16px未満にしないでください。** iOS は入力欄のフォントが 16px 未満だと
タップ時に自動ズームします。プラットフォームを見分けず、**全体を16pxに揃えて**あります。

> 以前は基本の規則に 13.5px と書いてあり、後ろの16pxの規則に上書きされて
> **どこにも効いていませんでした。** 実測して16pxだったので、死んだ数値は消しました。
> **入力欄の文字サイズを書く場所は1か所だけ**です。

### 見出しと本文を、大きさでは分けない

同じ 11.5px のまま、**太さ・色・字間**で階層を作ります。大きさで分け始めると段が増えます。

|役割|太さ|色|字間|
|---|---|---|---|
|区画の見出し（`.field > label`）|800|`--dim`|.05em|
|小見出し（`.sublabel`）|600|`--txt` 85%|0|
|本文・説明（`small` / `p` / `.note`）|400|`--dim`|0|
|チェックボックスの行|800|`--txt`|0|

### 例外は3つだけ

|例外|px|理由|
|---|---|---|
|ドロワーの見出し `h2`|16|画面の題名|
|説明の開閉ピル `.notetog`|10.5|本文より小さい補助操作。本文に埋もれないよう形（ピル）で区別する|
|会話カード本文|`--fs`（可変）|利用者が調整する対象そのもの|

---

## 2. クラスの語彙

新しい設定を足すときは、**この表にあるものだけ**を使ってください。

|クラス|何に使うか|
|---|---|
|`.field`|1つの設定のまとまり。縦に並ぶ。`gap:6px`|
|`.field > label`|その区画の見出し。**1区画に1つ**|
|`.field-head`|見出しの右に操作ボタンを置くとき|
|`.sublabel`|区画の中の小見出し|
|`small`|説明。`.field` の直下に置くと**長いときは自動でたたまれる**（§3）|
|`.note`|背景付きの注意。強調したいときだけ|
|`.keychk`|キー確認などの結果表示|
|`.btn` / `.btn ghost`|ボタン。**素の `<button>` を置かない**|
|`.settings-check switch`|チェックボックスの行|
|`details.adv`|畳める区画|
|`details.adv.panel-form`|**JSから注入するパネル**（§4）|
|`details.settings-help`|明示的にたたむ説明（§3）|

---

## 3. 長い説明はたたむ

仕組みが2つあります。**新しく書くときは (B) を使ってください。**

### (A) 自動 — `foldLongNotes()`

`#drawer .field` の直下にある `small` と `.note` は、**`NOTE_FOLD_MIN` 文字を超えると
自動で「説明」ピルが付いてたたまれます。** 何もしなくて構いません。
たたみたくないものには `data-fold="off"` を付けます（状態表示や警告）。

**この仕組みは `.field` の中しか見ません。** `details.adv` の中や `<p>` は対象外です。

### (B) 明示 — `details.settings-help`

```html
<details class="settings-help"><summary>説明</summary>
  <p>…</p>
</details>
```

見出しは **`.notetog` と同じピル**で描かれるので、(A) と見た目が揃います。

### 判断基準

**本文が3行を超えるならたたむ。** 常に見えている必要があるのは、
**その設定を選ぶために読む必要がある文**だけです。

---

## 4. JSから注入するパネル

`details` を作って `#p2` などへ入れるときは、**`adv panel-form` を必ず付けます。**

```js
var box=document.createElement('details');
box.className='adv panel-form';   /* panel-form が無いと <p> と <label> が16pxになる */
```

`panel-form` が付いていれば、素の `<p>` と `<label>` が本文11.5pxで出ます。
ボタンには `class="btn ghost"` を付けてください。

### `#id` にフォントサイズを書かない

```css
#turnDecisionSettings{font-size:11.5px}   /* ← やらない */
```

これをやると、次のパネルでまた別の値が書かれ、段が増えます。
**足りない規則があるなら `.panel-form` 側に足してください。**

---

## 5. 文言

- **1行目で何が変わるかを書く。** 背景や理由は2行目以降か、たたんだ中へ
- **既定値を必ず書く。**「（既定）」を選択肢の文言に入れる
- **失う物を書く。** 「声が替わります」「読み上げが数十秒遅れます」のように、
  選んだ結果として起きる不利益を省かない
- 数値は**単位と範囲**を添える（`ms`、`0〜120`、`空＝既定`）

---

## 6. 検査

`extension/tests/test-settings-style.cjs` が、**この文書の数値と実際のCSSの一致**を検査します。

- 本文・操作・入力の3段が、この表のとおりCSSに書かれていること
- JSが作る `details.adv` に `panel-form` が付いていること
- 設定パネルの `#id` にフォントサイズが書かれていないこと

**この文書を変えたら検査も落ちます。** どちらか片方だけ変わることはありません。
