# Web Speechの確定遅延：RCAと逐次試験

作成：2026-09-06 / アドオン Ver.1.0.2 / Build 20260906-chrome102-rca1

## 問題と到達点

HTML v1.37.5では動画再生中にWeb Speechの確定結果が得られる一方、アドオンでは途中字幕が長く続き、動画停止後に確定することがある。根本原因は未確定。本版は原因を測定する第1段階であり、確定遅延を修正済みとする版ではない。

前提を訂正する：両版ともWeb Speechには独自の発話区切り処理がない。したがって「独自処理がない」だけでは両者の差を説明できない。OpenAI gpt-live-transcribe経路の成功は別の観測である。同経路ではDuoのローカル区切り処理が3回動作しており、音声取得経路もtabCaptureへ変わっている。Web Speechとの差の原因を単独で証明しない。

## RCA管理表

| ID | 原因候補 | 現在の証拠 | 実装・試験 | 判定条件 | 状態 |
|---|---|---|---|---|---|
| R0 | 認識パラメータの相違 | ja-JP、continuous=true、interimResults=true、maxAlternatives=1、start(track)が一致 | 元タブにも実際の認識設定を記録 | 記録が一致する場合、これら明示設定の相違は除外。未指定の内部動作まで同一とはしない | コード上の明示設定差なし |
| R1 | 元タブの確定結果が本体へ届かない、または本体が捨てる | 従来ログは本体側のみで、元イベントとの照合が不可能だった | 元タブのraw-finalを独立保存。resultSeqと各転送段階の時刻、本体の受信・表示・除外を記録 | 元finalResults=0かつ途中結果ありなら、その試験の「未確定」は確定結果転送欠落では説明できない。元final>本体受信なら転送・セッション管理を調査。本体受信>表示なら除外・例外を確認 | 実装済み、実機結果待ち |
| R2 | 音声取得オプションの差 | HTMLの使用経路はaudio:true、アドオンはsuppressLocalAudioPlayback:falseを指定 | A=現行、B=audioだけtrue。残る映像・共有設定・実行ページは固定 | 同じ区間のA→B→AでBだけ改善する場合に有力。1回で原因確定とはしない。Bも失敗なら音声指定だけでは不十分 | 比較モード実装済み、既定A |
| R3 | 共有全体の条件・自己タブ取得・認識実行ページの差 | HTMLはDuoページ、アドオンはYouTubeページ。selfBrowserSurface等も違う | R1/R2の結果後、別のHTTPSページでの認識や共有条件を分離した試験を設計 | 同じ入力を保って実行場所のみ変更できる試験が必要。複数変更を一度に適用しない | 未実装 |
| R4 | 音声処理・音声形式の差 | 成功アドオンの共有Trackは48kHz/1ch。過去のtabCapture失敗は96kHz/2ch。これだけで因果は不明 | sampleRate、channelCount、echoCancellation、noiseSuppression、autoGainControl等の実測設定を追加 | 同一音源で条件統制後に判断。値未提供はnullとし、OFFと解釈しない | 観測実装済み |
| R5 | BGM・連続発話に対する認識側の区切り判断 | 途中結果が継続し、停止後に確定した観測。HTML成功との差を単独では説明しない | 同じ区間と再生速度、同じ試験長で比較。speechend/soundend/audioendと音量指標を記録 | 無音指標とブラウザの発話終了は別。文末で確定しない条件の再現を確認する | 観測実装済み |
| R6 | 旧スクリプト混在で違う版を試験している | 過去のInvalid capture constraints時に旧MAINスクリプト残存が問題となった | content側とMAIN側のbuildを開始前に照合し、不一致なら再読み込みを案内 | 最新buildで統一された試験のみ採用 | 混在検知実装・模擬テスト済み |
| R7 | 認識セッションの再起動方法の差 | HTMLは同一認識オブジェクト再利用、アドオンは新規生成。待機時間も異なる | generationとrecognition-endを記録 | 最初のgeneration=0から遅ければ再起動方式だけでは説明できない。再起動後だけなら次に変更 | 観測実装済み |

## 第1試験：まずAだけ実施

担当：ユーザー＝Chrome実機操作、アシスタント＝ログ照合と次の修正。所要約2分＋更新操作。

1. 使用中の拡張フォルダへZIP内のファイルを上書きし、chrome://extensionsで再読み込みする。別フォルダで再登録すると設定の保存先が変わる可能性があるため、既存フォルダを使用する。
2. Duo本体と動画タブを再読み込みする。動画タブを字幕対象に設定し直す。表示がVer.1.0.2 / Build 20260906-chrome102-rca1であることを確認する。
3. 設定→音声：音声認識=Web Speech、共有音声STT経路=直接Web Speech、Web Speech取得=画面共有、確定遅延の比較=A：現行の音声条件。
4. 入力A=共有音声、入力B=OFF、言語切替=手動、言語A=日本語、読み上げ=OFF、翻訳=なし。同じ動画の試験開始位置を記録する。APIキーの変更は不要。
5. 認識を開始し、対象ページの「音声共有を選んで開始」で、その動画タブと音声共有を選ぶ。動画を同じ位置から60秒間再生する。
6. 動画だけ一時停止し、Duoは認識中のまま15秒待つ。字幕が再生中・停止後のどちらで確定したかを記録する。15秒以内に確定しなくても結果として採用する。
7. Duoを停止し、2秒ほど置いて既存の「診断ログを書き出し」を押す。対象タブやDuo本体を閉じたり再読み込みする前に出力する。
8. ログと「動画の試験区間、再生中に確定したか、停止後に確定したか」を送る。

## 結果を受けた次の一手

- 元タブに確定あり、本体にない：R1の経路をresultSeqで特定し、転送・セッション処理を修正する。Bの試験は後回し。
- 元タブに確定なし、途中結果あり：次は同じ区間でBを試し、その後Aへ戻して再試験する。Bはaudio指定1項目だけの変更である。
- 元タブにも本体にも確定あり、表示なし：本体のnoise/echo/emptyと未処理件数を確認する。
- 診断元記録が回収できない：判定保留。ログの回収エラー・対象タブID・buildを先に直す。回収失敗を確定ゼロとして扱わない。
- Aで再現しない：R1を解決済みとはしない。試験条件と旧版混在の有無を確認し、同じ区間で再現性を調べる。

## 診断データの読み方

診断ログ末尾に「Web Speech RCA」を追加した。通常の動作ログとは別に以下を保存する。

- source.snapshot.sessions：対象タブ自身の元記録。認識結果通知の転送経路とは別に、scripting経由で出力時に読み取る。
- resultEvents：onresultを受けた回数。finalResults：今回変更されたresultIndex以降でisFinal=trueだった結果数。通知回数と結果数は異なる。
- eventsのraw-final：resultSeq、generation、resultIndex、各結果のisFinalと文字数、sourceAt。元記録には認識テキストを保存しない。
- app：同じsessionIdの本体処理。finalEventsにはsourceAt→contentAt→workerAt→bridgeAt→appAtを記録する。端末時刻変更があれば時間差は無効。件数一致だけでは順序や内容の一致を保証しないため、resultSeqも照合する。
- applied：確定字幕のrenderが戻った回数。empty/noise/echo：除外理由別の件数。受信数とこれらの合計に差があれば処理途中の例外などを調査する。render完了は画面の見え方や読み上げ成功を保証しない。
- ackedFinalResults：本体ブリッジが応答した確定数。字幕反映とは別。停止直後は応答が戻る前にセッションが終了することがあるので、この値だけで欠落と判定しない。
- level：音量RMS、しきい値0.035以下の時間、AudioContextの状態。50msごとの短い観測であり、人の発話終端や完全な無音を保証しない。認識への制御には使わない。

保持範囲：元記録・本体記録とも直近6セッション、詳細イベントは各160件。累計数は同じセッション内で保持する。元記録は対象ページの再読み込みで、本体記録はDuo本体の再読み込みで消える。対象を別タブへ切り替える前に出力する。通常の診断ログには従来どおり会話テキストが含まれる。

## 実装範囲と検証

今回：R1の観測、R2の単一条件比較、R4/R5/R7の観測、R6の混在検知を実装。共有音声をマイクへ切り替えたり、認識結果を独自に早期確定する処理は追加していない。既存の認識・翻訳・読み上げ設定は保存したまま、比較条件の既定はA。

検証：静的検証74項目、ブラウザAPIを模擬した動作テスト23件が成功。元記録の独立保持、確定の転送・応答、A/B間の変更がaudioのみであること、旧版検知、診断回収タイムアウト、本体での除外理由を検証した。

この環境に実機Chromeと音声認識サービスの実行環境がないため、YouTube再生中のisFinal発生と遅延改善は未検証。原因候補の最終判定は次の実機ログで行う。
