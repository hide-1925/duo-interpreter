# 次期仕様の実装状況

HTML v1.49.4 / Chrome・Edge 拡張 v1.4.15 — 2026-09-22

（判断層を追加した版。既定の `turnDecisionMode=off` では HTML v1.47.1 と同一挙動です。以下の v1.47.1 への言及は、その比較基準としての参照です。）

添付の「Teams Conference / Many-to-Many Speaker Attribution & Conference Audio Routing」に対応。Phase 1〜3 のコード実装とローカル検証を完了。**Phase 4 の実Teams会議での音声送出は v1.4.8 で確認済みです。**

|対象|実装内容|検証範囲|
|---|---|---|
|Preset|Web / Many to Many の会議Endpoint、用途説明、ホバー・フォーカス400ms、タッチ向け説明|ブラウザー確認|
|Teams Speaker Adapter|独立したDOMセンサー。字幕本文を取得せず、参加者名・ID・発話状態のみ送信|模擬Teams DOMで開始200ms・終了300ms、名前遅延、同名参加者を確認|
|Registry / Timeline|名前に依存しないID、セッション分離、epoch時刻の発話区間、ハートビート|単体・ブラウザーテスト|
|Attribution|発話時間との重なり、前400ms・後600ms補正、信頼度、複数話者・不明・判定中、遅延情報による既存カード更新|合成イベントで確認。実会議の精度は未測定|
|手動修正|カードの話者メニュー、前後話者・参加者一覧・不明・自動に戻す、手動ロックとrevision|カード表示・エクスポートを確認|
|議事録・ログ|speaker情報とrevision、議事録schemaVersion 2、指紋への反映、CSV追加列、診断ログ|単体・ブラウザーテスト。外部AIによる議事録再生成は未実行|
|Conference Audio Mode|接続中のスピーカーボタンを on → mix → off → on に変更、Drawer同期、接続初期値on、切断時リセット|ブラウザーと通信制御テスト|
|自動TTS|offで停止・待ち行列破棄、認識と翻訳は継続、再開時に過去分を再生しない|単体・ブラウザーテスト|
|Manual Replay|手動カード読み上げを会議送出対象から除外|送出ポリシー・ジョブのテスト|
|Remote Relay|既定は自分のみ。明示選択した参加者の自動・翻訳音声のみ送出可能。off/onで選択維持|送出条件を単体テスト|
|Mixer|mixだけWeb Audioで合成。元音声70%、TTS100%、各0〜100%、コンプレッサーによるピーク抑制|実Web Audio・合成音声で確認|
|Teams送出|onはTTS、offは元マイク直接。元マイク追跡、mute優先、復帰、失敗時停止|模擬senderと実Web Audioで確認|
|実Teams調整|診断項目を実装。実会議ログ5本（v1.4.2〜v1.4.6）で4件の不具合を特定し修正|**実機確認済み。on / mix / off の3モードで送出を確認**|

## 実機で確認できたこと（2026-09-21 / 拡張 v1.4.8）

- Web版TeamsへのTTS送出。**on（TTSのみ）/ mix（原音+TTS）/ off（原音のみ）の3モードすべて**で動作。
- 送信トラックの選定（`mic-in-processed-mix`）とマイクの識別。識別は体感で高速。
- 読み上げはAivisで確認。

実会議ログ5本から特定・修正した不具合は以下（詳細は `extension/history/`）。

|版|不具合|
|---|---|
|v1.4.3|出所不明のトラックを外部由来と誤判定し、候補が0件になる|
|v1.4.4|マイクと受信音声が同居する送信経路を一律に除外していた|
|v1.4.5|多段Web Audioグラフでマイクの証拠が1段で消えていた|
|v1.4.6|モード切替の2回目以降が空振りし、UIだけが新しいモードを表示していた|
|v1.4.7|ブリッジのICE収集2段分が接続期限を食いつぶし、無言でタイムアウトしていた|

## 残る実機確認

- **長い会議での接続維持。** Teamsのページ遷移でセッションが切れる事象を観測しています（ログでは89秒・111秒で切断し、合計801秒が無接続でした）。切断時に元のマイクへ戻すのは正しい動作ですが、気づきにくいため自動再接続の要否を検討します。
- 実Teamsの現在のDOMで参加者名・発話枠を検出できるか。画面構成や仮想化でタイルが存在しない場合は不明表示になります。
- 実Teamsのマイク差替え・デバイス変更・再接続。ミュート優先と復帰はコード上実装済みですが、実機での網羅確認は未了です。
- STTの発話区間とDOMイベントの実遅延。録音単位のSTTや連続発話のカード分割は概算時刻を使うため、短い交代や発話重複の精度は実ログで調整が必要です。
- Aivis以外のTTSサービスでの会議中の確認。
- 実ブラウザーによる自動E2E。`extension/tests/test-browser.cjs` は playwright が必要で、成功実績がありません。今回の確認はすべて人手による実機操作です。

真の音声分離・声紋識別は実装対象外です。混ざった音声から個別の音声を分離する機能ではありません。話者メタデータが取得できなくても認識・翻訳は継続します。

検証結果の詳細は `検証結果.json`、更新方法は `更新手順.md` を参照してください。

## Jev適用 Phase 0 / Phase 1（2026-09-21）

開発仕様書 v2 の Phase 0（計測基盤）と Phase 1（契約と parity）を実装。**本番動作は変わりません。**
`turnDecisionMode` の既定は `off` で、そのとき境界判定と Floor Gate は v1.47.1 と同一の値を返します。

|対象|実装内容|検証範囲|
|---|---|---|
|判断層の契約|`TurnDecision`。`{length, reasons, waiting}` を正規化契約とし、既存 `segDecision` / `segSemanticDecision` を RulesProvider として常時搭載|単体28件|
|OFF互換|`turnDecisionMode=off` で `boundary` が RulesProvider の戻り値を**同一参照で**返す。言語別設定を active にしても変わらない|単体（参照一致で検査）|
|INV-08|判断の不在はゼロコスト。cache に結果が無ければ待たず Rules へ進む|単体|
|INV-09|Floor 待ちは `turnFloorMaxWaitMs`（既定3000ms）を超えて保持しない。期限時は speak または drop|単体|
|ProsodyAnalyzer.peek|発話途中の非破壊スナップショット。`finalize()` は frames をリセットするため判断層からは呼べない。終端ピッチ勾配・エネルギー減衰・ポーズ構造を相対値で返す|単体（非破壊性を検査）|
|prosody のゲート|`turnDecisionMode=off` では peek を呼ばない。60ms周期のYIN抽出を無駄に走らせない|単体（呼び出し回数0を検査）|
|silenceMs の契約|meter不明を `null` で渡す。Rules内部の `-1` は契約へ漏らさない|単体（0/null/-1 を区別）|
|境界候補の安全性|候補は安定prefix以内。候補外offsetと安定prefix超過は採用しない|単体（注入offsetを拒否）|
|speakerKey|Teams speakerId は session単位のHMAC相当で仮名化。平文IDを外部へ出さない。無ければ seat|単体|
|送信の最小化|`turnDecisionContextTurns` の既定は 0。原文は `turnDecisionRawLog` が明示onのときだけ記録|単体|
|カウンタ分離|`SEG.committedBySource` / `correctedBySource`。判断層をoffへ戻したあとの累積比の残留と baseline 汚染を切り分ける|単体|
|turn trace recorder|`turnTraceMode=record` のときSTT・VAD・話者・first audio・Rulesの決定を時刻付きで記録。off では1行も積まない|単体|
|replayer と自動ラベル|`extension/tools/turn-trace-replay.js`。記録時のRulesの決定を再現できるか検査した上で、premature commit を自動ラベルで数える|単体8件|
|2ファイル同期|`index.html` と `extension/app.js` の seg系・判断層・設定既定値が空白無視で一致することを検査|単体19件|
|テストランナー|`extension/tests/run-all.cjs`。決定論的スイートをgate、実ブラウザE2Eを別扱い|全スイート実行|

### 実ブラウザ自動E2Eが通るようになりました

これまで `extension/tests/test-browser.cjs` は成功実績がありませんでした。原因は2点で、いずれも製品側ではなくテスト側の問題でした。

- ICE gathering の **完了** を待っていた。STUN へ到達できない環境では完了しないため必ず5秒で落ちる。製品側の中継は v1.4.7 以降は最初の候補で応答する仕様（`test-bridge-target.cjs` の契約）なので、テストも候補1つで進めるようにした。
- mDNS で隠された `.local` 候補は同一ページ内のループバックで解決できない。`--disable-features=WebRtcHideLocalIpsWithMdns` で実IPを出させた。

実行方法（このリポジトリには package.json が無いためグローバル解決に頼ります）。

```
node extension/tests/run-all.cjs                                  # 決定論的スイート
NODE_PATH=$(npm root -g) node extension/tests/run-all.cjs --with-e2e
```

### Web Speech でタブ／システム音声が取り込めなくなっていた不具合の修正

Web または Many to Many プリセットで音声認識に Web Speech を選ぶと、開始時に
「Webプリセットの共有音声にはOpenAI / Groq / Geminiの音声認識を選んでください」で止まっていました。
v1.47.1 から存在する不具合で、判断層とは無関係です。原因は2つあります。

- **capability テーブルが実装と矛盾していた。** `STT_ADAPTER_CAPABILITIES.webspeech` が
  `arbitraryTrack:false` と宣言していましたが、`WebSpeechTrackEngine` は Chrome 135+ の
  `SpeechRecognition.start(audioTrack)` で共有音声 Track を直接認識するために存在します。
  能力はあるのにテーブルが「無い」と言っていました。
- **判定が広すぎた。** `duoValidateInputs()` は system-audio の endpoint が *有効かどうか* だけを
  見ており、それを **認識源に使うのか** を見ていませんでした。Web プリセットでマイクを認識源にして
  会議音声は TTS の送出先にしか使わない構成まで巻き込んで止めていました。

修正後は、A／B のどちらかが認識源として共有音声に束ねられている場合だけ検査し、経路ごとに
必要な能力を見ます。`direct` は Track を直接渡すので `arbitraryTrack`、`api` は Blob を送るので
`systemAudio`、`vb` は仮想入力デバイスなので通常のマイク扱いでどの adapter でも通ります。
開始できない実機は `WebSpeechTrackEngine` 側が理由を出して止まるため、テーブルで先に塞ぎません。

`extension/tests/test-input-validation.cjs`（10件）で組合せを固定しました。

### 設定 UI を追加しました

⚙→音声 に「発話交代の判断層（実験）」を追加しました。判断モード、言語別の上書き、音響特徴の送信、
送る直近ターン数、読み上げ開始の待ち上限と期限時動作、trace 記録、原文の記録、trace の書き出しと破棄、
状態行を持ちます。DevTools での `localStorage` 操作は不要になりました。

markup は `index.html` と `extension/app.html` の両方へ置くと同期先が3つになるため、`duoNextInstall` と
同じく **JS から注入**しています。`bindSettings()` は読み込み時に一度だけ走るので、注入後に `simple()` を
呼んで既存の永続化経路へ相乗りし、独自の onchange は書いていません。

Phase 2 まで意味を持たない設定（provider / model / baseUrl / apiKey）は UI に出していません。
`localStorage` で設定できますが、向ける先がまだ無いためです。

`extension/tests/test-settings-ui.cjs`（8件）で実ブラウザでの生成・初期値反映・永続化・記録の
開始停止・状態行・page error なしを固定しました。

### Phase 2 の Provider adapter 層

`TurnProviders` を追加しました。判断契約を Duo が所有し、ベンダーを adapter で差し替えます。
既定の `turnDecisionProvider='rules'` では registry を一切引かないので、**shadow でもネットワークは発生しません。**

|要素|内容|
|---|---|
|正規化|schema外の選択肢、提示していない候補への確率、確率合計の破綻、NaN・範囲外・型違い、巨大応答、非JSONを必ず拒否。通らなければ捨てて Rules へ|
|`jev-direct`|`POST /v1/systemone`。公式リファレンスどおり `{state, model, questions}` を送り、`answers` を自分の質問IDで引く。ZDR と no-training をヘッダで明示送出。HTTPS のみ許可。キー未設定なら要求前に拒否。active では model の alias を拒否（§8.5 の version pin）|
|`local`|ブラウザ内の合成器。遅延ゼロ・外部送信なし・レート制限なし。**学習済みの重みが無いあいだは動きません**|
|circuit breaker|provider＋言語単位。30秒内3回の障害で OPEN 60秒、half-open は成功2件で復帰|
|発火制御|speakerKey ごとに同時1件、新 revision で abort、同一 bucket は300ms以内再送しない。音響 bucket は100ms刻みで量子化|
|401／422|設定エラーとして自動再試行せず、判断モードを off へ戻して通知|
|`questionSetHash`|instructions 本文のハッシュ。文面が変われば校正記録を無効として扱う|

`local` が手置きの係数で動かないのは意図的です。仕様書 §9.1 は「閾値を手置きせず学習する」と
定めており、係数を決め打ちすると校正の意味が消えます。重みは trace から学習します。

```
node extension/tools/turn-trace-replay.js turn-trace-....json --fit --out weights.json
```

**言語ごとに独立して**自動ラベルからロジスティック回帰で重みを当て、前半で学習・後半で holdout
評価し、**precision 目標から閾値を逆算**します（既定 0.98、`--precision` で変更。最低件数は `--min`、
既定50）。目標に達しない言語、件数が足りない言語、holdout が片方のラベルだけの言語は `skipped` に
理由付きで出し、重みを出しません。**片方の言語だけ合格することがあります。**

言語を分ける理由は、公式ドキュメントが「日本語の本番ワークロードは英語の閾値を流用せず独自の
ラベル付き評価が必要」と明記しているためです。話者Aが日本語・話者Bが英語の会議 trace を1つの
モデルへ混ぜると、この注意をそのまま踏み抜きます（仕様書 §9.3）。

出力は `{version, languages:{ja:{...}, en:{...}}}` 形式で、⚙ の設定または `localStorage` の
`di.tdWeights` へ入れると `local` が動きます。`local` は発話の src 言語で重みを引き、
**その言語の重みが無ければ他言語のものを流用せず拒否します。** 閾値も学習時に逆算した値を使い、
実行時に決め打ちしません。

`test-turn-providers.cjs`（22件）で拒否すべき応答の網羅、ZDR ヘッダ、circuit breaker、
発火制御を固定しました。`test-turn-trace-replay.cjs` に学習側の5件を追加し、
**学習側と推論側の特徴量の並びが一致すること**も検査しています（ずれると係数が別の特徴へ当たる）。

実装中に自分のテストが1件バグを見つけました。`Number(null)` が 0 になるため、欠落した Noul 値が
「確率0」として通っていました。読み上げを永久に抑止したり誤って開始したりする経路だったので、
型で弾くように直しています。

### API 契約を公式リファレンスに合わせました

Phase 2 の初版は `docs.typesafe.ai` に到達できず検索結果から組んだため、リクエストとレスポンスの
形状が間違っていました。公式リファレンスを突き合わせて直しました。

|直した点|誤り|正しい契約|
|---|---|---|
|instructions の位置|トップレベルに1つ|**question ごと**に `instructions`|
|choice の選択肢|`options` 配列|**`criteria`** の「選択肢 → 説明」map（最大255）|
|noul の criteria|送っていない|任意だが `{true, false}` で yes/no の意味を書ける。送るようにした|
|`model`|未設定なら省略|**必須**。既定 `jev-latest`、active では alias を拒否|
|レスポンス|`{boundary, turnState, safeToSpeak, ...}` を直接期待|**`answers` を自分の質問IDで引く**。choice は `choice`／`probabilities`／`confidence`、noul は `noul` のみ|
|`usage`|捨てていた|`input_tokens` を残す。レート制限がアカウント単位なので実測が必要|
|never-retry|401／422 のみ| 400／401／403／404／422。既知の不一致として本来422の検証エラーが400で返ることがある|

**旧実装のままでは、正規化が実際の応答をすべて拒否していました。** 契約テストで固定しました。

あわせて**候補IDの重複バグ**を直しました。`criteria` は map なので、同じIDが2つあると衝突して候補が
消えます。旧実装は文末が複数ある文章で `C_SENTENCE` を重複生成していました。offset ごとに一意な
IDを振り、左右24文字の文脈を添えてモデルへ意味を渡すようにしています。同一 offset の候補は
重複排除されます。

**instructions は state のフィールドをバッククォートで参照して意味を書いています。** Jev はテキストしか
見ないので、`prosody.terminalPitchSlope: -0.25` のような数値は説明がなければ解釈できず、ノイズとして
送られるだけになります。初版はこれを書いていませんでした。あわせて Provider へ送る prosody を
`TURN_PROSODY_SENT` の6フィールドに絞り、**送るのに説明のないフィールドがあればテストが落ちる**ように
しました。`silenceMs` の null が 0 と別であることも instructions に明記しています。

**instructions と criteria は英語で書いています。** 公式が「英語が最も強い言語で、CJK は受理される
が精度は同等でない」と明記しているためです。判断対象の日本語は `state` と候補の左右文脈として
原語のまま渡るので情報は失われません。この選択自体は shadow で検証します。

### 境界の閾値を confidence から probabilities へ移しました

公式の `/confidence` に算出式が載っていました。`confidence` は確率分布から導く統計量で、
**選択肢数 N に依存します**。式は `(N × p_max − 1) / (N − 1)` です。`/primitives/choice` の
実例（4択で `p_max = 0.40` → `confidence` 0.20）が式と小数点以下まで一致するので、これは近似では
なく定義とみて差し支えありません。

`boundary_choice` の N は HOLD ＋ 候補で**リクエストごとに変わります**（候補の上限を外したので、
なおさら変わります）。同じ `p_max = 0.7` でも
N=2 なら 0.40、N=6 なら 0.64 です。つまり仕様書 §6.4 の「boundary confidence 下限 0.60／0.70」は、
モデルの確信度ではなく **Duo が何件候補を作ったか**で合否が変わる閾値でした。設計として成立しません。

そこで境界の判定を `probabilities` 側へ移しました。候補数に依存しない量だけを見ます。

|判定|使う値|理由|
|---|---|---|
|HOLD するか|`P(HOLD)` の上限（en 0.15 / ja 0.10）|候補数に依存しない|
|commit するか|選ばれた候補そのものの確率の下限（en 0.70 / ja 0.80）|同上|
|発話継続|`turn_state` の `confidence` 下限 0.50 と `P(CONTINUING)` 上限|**選択肢が4つ固定**なので confidence が比較可能|
|言い直し|`repair_likelihood` の上限（en 0.20 / ja 0.12）|Noul は値そのものが確率|
|音響 guard|最低無音（en 250ms / ja 450ms）。meter 不明は STT final まで待つ|null と 0 を区別する|
|ヒステリシス|rule と不一致なら N 回連続で同じ offset を支持したときだけ commit（ja は2回）|閾値付近の振動で確定させない|

`UNKNOWN` は Rules へ落とします。公式が「Noul の 0.5 は中くらいではなく判断がつかない」と
書いているのと同じ扱いです。

N 依存そのものをテストに残しました（`the documented confidence statistic moves with the option count`）。
固定 0.60 の下限が同じ確信度を候補数で割ってしまうことを数値で示しており、将来 confidence 下限へ
戻そうとしたときにその記録が残ります。

**これらは校正前の出荷値です。** §9.1 の学習で置き換える前提で、ASSIST 以上へ上げる前に校正が必要です。

## 候補を絞らず、HOLD と切る位置を同じ項目立てで説明する（公式 `/primitives/choice` に合わせる）

公式の Choice のリファレンスに、実装と食い違う記述が二つありました。

### 1. 候補の絞り込みをやめた

> A Choice question accepts up to 255 options, and adding options costs a few tokens each, so give the
> model the full list ... rather than a shortlist. / The model cannot choose an omitted value.

`TurnDecision.candidatesOf` は候補を**5件で打ち切って**いました。実際にはこれが順番に効くため、
文の多い発話では構造上いちばん重要な候補が押し出されていました。

|候補|意味|以前の挙動|
|---|---|---|
|`C_RULES`|Rules が出した切り位置|1件目なので残る|
|`C_SENTENCE`|文末|最大4件まで|
|`C_STABLE`|安定 prefix の端|**文末が4件あると落ちる**|
|`C_FULL`|STT final の文末|**同上。final でも選べない**|

つまり「1発話に文が4つ以上ある」ときの final で、発話全体を切るという正解が選択肢から消えて
いました。公式の言う「並べなかった値はモデルが選べない」がそのまま起きる状態です。

直し方は二つです。**構造上の候補（`C_RULES`／`C_STABLE`／`C_FULL`）は数を問わず必ず載せる**ように
分離し、**文末候補は絞らずに全部渡す**ようにしました。上限は `TURN_MAX_CANDIDATES=64` を安全弁として
残しています（HOLD を足しても公式の 255 に遠く届かない値です）。`currentText` が 800 字なので
実際の件数は数十件で収まり、超えるのは病的な入力だけです。そのときは末尾側を残します。切る判断に
効くのは末尾だからです。

offset ごとの重複排除は残しています。`C_STABLE` と `C_FULL` が同じ位置になるのは正常なので、
その場合は 1 件です。

### 2. 混同しやすい選択肢は項目立てして説明する

> When two options are similar and the model keeps confusing them, ... structured criteria with fields
> you choose.

HOLD と「ここで切る」は、この設計で最も混同しやすい対です。どちらも「まだ足りない情報がある」
ように見える局面で競合します。そこで 1 文の説明をやめ、同じ項目立てで書きました。

|選択肢|`what`|`not_for`|その他|
|---|---|---|---|
|`HOLD`|まだどこも切らない|短いだけ・`silenceMs` が null だけを理由にしない|`examples` に接続詞・助詞・filler・言い直しの前半|
|`C_*`|`before` と `after` の間で切る|`before` が節の途中／`after` が同じ節の続きなら選ばない|`before`／`after` に前後24字|

前後の文脈はこれまで 1 本の文字列に埋め込んでいましたが、`before`／`after` という別フィールドに
分けました。フィールド名は呼び出し側が決めてよいと公式に明記があります。

`questionSetHash` は `HOLD_CRITERION` が文字列からオブジェクトに変わったので `JSON.stringify` を
通すよう直しました。ハッシュは変わります。指示文が実際に変わったので、§9.1 のとおり以前の校正値は
無効として扱うのが正しい挙動です。

### 検査

- `HOLD and each cut candidate are described with the same structured fields` — HOLD と全候補が
  同じ項目を持つことを、実際に送る body から検査します。
- `the structural candidates survive a text full of sentence ends` — 文末が40個ある final の発話で
  `C_FULL` と `C_STABLE` が残り、候補が5件で打ち切られないことを検査します。

## 判断の経路を宣言で足せるようにし、設定UIと診断へ出す

設定UIに**経路の選択とAPIキーの欄が無く**、`turnDecisionProvider` と `turnDecisionApiKey` は
localStorage を直接触らないと設定できませんでした。実質 Jev へ繋げない状態でした。
診断ログにも判断層の行が1つも無く、ログを見ても rules のままか判断層が効いているのか
読み取れませんでした。両方を直し、あわせて経路の足し方を宣言的にしました。

### 経路レジストリ

`TurnProviders[id]` で直接引いていたのをやめ、`ROUTES` へ分離しました。1経路が書くのは
**vendor / endpoint / 認証ヘッダ / body の組み立て / 応答の取り出し**だけです。
タイムアウト・中断・サイズ上限・model pin の検査・再試行しない status・回線遮断は
`httpProvider` と `observe` が共通で持ちます。

| 経路 | vendor | 入口 | 確率の出どころ | 形式 |
|---|---|---|---|---|
| `rules` | — | 送信なし | — | — |
| `local` | — | 送信なし | `post_calibrated` | — |
| `jev-direct` | `typesafe` | `/v1/systemone` | `native_calibrated` | 確認済み |
| `jev-openrouter` | `openrouter` | `/v1/chat/completions` | `vendor_reported` | **未確認** |

OpenAI や Claude が同等の判断APIを出したら、`ROUTES` に1件足すだけで**設定UIの選択肢にも
診断にも自動で出ます**。UIは `TurnProviders.list()` から作るので、足し忘れが起きません。
`every declared route carries the full contract` が、契約を半端に宣言した経路を落とします。

分離のついでに実害も1件消えました。以前は `TurnProviders[id]` を引いていたので、
`turnDecisionProvider` に `normalize` のようなメソッド名を入れると関数が経路として扱われました。
`get()` は `hasOwnProperty` で registry だけを引きます。

### vendor 別のAPIキー

TypeSafe直と OpenRouter では別のキーが要ります。1本の欄を使い回すと経路を切り替えるたびに
貼り直しになるので、`turnDecisionKeys` に **vendor名 → キー** の map で持ちます。
旧 `turnDecisionApiKey` は読み出しの後方互換にだけ残しました。

- `embed` を持たないので設定埋込みHTMLに出ません
- `portable:false` なので設定の書き出しにも乗りません
- 入力欄は `type="password"`、保存後は**値を画面へ戻さず**「設定済み N文字」とだけ出します
- 診断ログも同じで、**キーの値は出しません**

`no decision-layer key rides along in anything portable` は実際の `exportData()` を通して
検査します。試しに `turnDecisionKeys` を `portable:true` にすると
`a key reached the exported settings` で落ちることを確認済みです。

### OpenRouter 経路は未確認です

公式が「OpenRouter 経由では 32k」と書いているので Jev が OpenRouter に出ているのは確かですが、
**どの入口でどの形式を受けるかの資料をこの環境から取得できません**（`openrouter.ai` は
この作業環境のネットワーク方針で遮断されます）。OpenAI 互換の `chat/completions` に
`response_format: json_schema` で `answers` を返させる前提で組みました。

- `verified:false` を宣言し、**設定UIの選択肢にも「（未検証）」と出ます**
- 文脈上限を `maxStateChars` で直叩きより小さくし、超えたら**送らずに** Rules へ落とします
  （途中で切ると候補の offset と本文がずれ、存在しない位置を選ばせてしまうため）
- 形式が違っていても、直すのは `ROUTES['jev-openrouter']` の `path` / `body` / `parse` の3つだけです

### 確率の出どころで閾値を変える

`probabilitySemantics` は記録されるだけで**判定に使われていませんでした**。閾値は
`native_calibrated` な確率の上で決めた値なので、校正されていない確率を同じ閾値に通すと
確信度の水増しをそのまま採用します。`thresholds(lang, semantics)` が校正外の経路では
commit 側（`chosenMin` / `turnConfMin` / `confirm` 回数）だけ厳しくします。
**待つ側（`holdMax`）は緩めません。**そこを緩めると「切らない」判断が減り、逆方向に危険だからです。

### あわせて直した死に枝

`pick()` は `P(CONTINUING)` の上限で commit を止める分岐を持っていますが、`normalize()` が
`turnState.probabilities` を落としていたため、**この分岐は一度も実行されませんでした**。
boundary と同じ厳しさで検査して通すようにしました。

### 診断ログ

`発話交代の判断層` `判断の経路` `判断の内訳` の3行を足しました。モード・言語別・経路・モデル・
キーの有無と文字数・確定の rules / provider 内訳が出ます。

## 版管理を検査で縛る（v1.48.0 を名乗る別物を出した事故の後始末）

判断層の経路レジストリと設定UIを入れたとき、`APP_VERSION` を `v1.48.0` のまま据え置きました。
結果、**中身の違う2つのビルドが同じ版・同じビルドIDを名乗る**状態で main に載りました。

| コミット | 名乗り | ビルドID | `index.html` の実体 |
|---|---|---|---|
| `454a232`（リリース済み） | `v1.48.0` | `…v1480-turn-decision` | `4e16d461…` |
| `1bdeb9b` | `v1.48.0` | `…v1480-turn-decision` | **`d689c7fc…`** |

診断ログには版とビルドIDが出ますが、**この2つは区別できません**。利用者が送ってくれたログを
見ても、判断層のUIがある版なのか無い版なのかが読めない状態でした。

`v1.49.0` として名乗り直し、拡張も `1.4.11` へ上げました。拡張の zip には `app.js` が入るので、
HTML の版が変われば拡張パッケージの中身も変わります。**対になる版を `versions.json` に記録します。**

### versions.json

出荷した版ごとに `index.html` と `extension/app.js` の SHA-256、対になる拡張の版、コミットを
記録します。**過去の版は手書きせず git から実測しました。** 手で書くと、まさに今回の取り違えが
記録側で再発します。

|版|拡張|`index.html` の SHA-256（先頭）|
|---|---|---|
|v1.47.1|1.4.8|`09d2a9f9f0892d77`|
|v1.48.0|1.4.9|`4e16d4616ca1dc2d`|
|v1.49.0|1.4.11|`a3bd0bc6431998f3`|

### 検査（gate）

`tests/test-release-manifest.cjs` を gate に入れました。規則はひとつです。

> いま名乗っている版が `versions.json` に記録されているなら、
> 記録された SHA-256 と実ファイルが一致しなければならない。

**中身を変えたら版を上げるしかなくなります。** あわせて次も検査します。

- `APP_BUILD` が版の名前を含むこと（版だけ上げてビルドIDを据え置く取り違え）
- `manifest.json` の版が、その HTML 版と対になっている版であること
- その拡張版の `README-v*.md` が存在し、対になる HTML 版を明記していること
- 2つの版が同じ `index.html` を記録していないこと（どちらかが誤記である証拠）
- `commit` を記録した版は、実際に git のその地点の中身と一致すること（記録の後付け改変を止める）

**3つの失敗を実際に再現させて、それぞれ落ちることを確認済みです。**

|再現した失敗|検査の反応|
|---|---|
|中身を変えて版を据え置く（今回やった失敗そのもの）|`no longer matches what v1.49.0 recorded. Bump APP_VERSION…`|
|版だけ上げてビルドIDを据え置く|同上で停止|
|別の版に同じ中身を記録する|`two versions record the same index.html`|

`受入確認手順.md` に「出荷時に両ファイルの SHA-256 を一組で保存してください」と書いておきながら
自動化していなかったのが原因です。手順書に書くだけでは守られません。

## 実機で判断層が到達できなかった（v1.49.0 の実測）

v1.49.0 を shadow にして Jev のキーを入れた実会議のログで、判断層が一度も届きませんでした。

```
turn-decision-request  {"provider":"jev-direct","language":"ja","candidates":0,"textChars":6}
turn-decision-fallback {"reason":"Failed to fetch","elapsedMs":140}
turn-decision-fallback {"reason":"Failed to fetch","elapsedMs":156}
turn-circuit-open      {"provider":"jev-direct","until":...}     ← 3回で遮断
turn-circuit-half-open {"provider":"jev-direct"}                  ← 60秒後に再試行
turn-circuit-open      {"reason":"Failed to fetch"}               ← また失敗
```

### 安全側は設計どおりに動きました

遮断が開いたあとも会議は途切れず、**Rules が54回確定して訂正0件**で走り切っています。
INV-08（判断の不在はゼロコスト）は実地で成立しました。壊れたのは**理由の伝え方**です。

### 原因：記載の無いヘッダを私が足していた

`X-TypeSafe-Zero-Data-Retention` と `X-TypeSafe-No-Training` を常に送っていました。
**公式リファレンスに記載が無く、こちらで足したものです。**

別オリジンへのリクエストは、サーバが `Access-Control-Allow-Headers` にヘッダ名を
並べていなければ preflight(OPTIONS) の段階で止まり、本リクエストは発射されません。
ブラウザはその区別を教えず、`TypeError: Failed to fetch` だけを投げます。
140ms 前後で失敗しているので**サーバまでは届いており、ブラウザが結果を捨てた**
形です。CORS の典型的な症状です。

実在を確認できていないヘッダのために疎通を落とすのは本末転倒なので、既定では送りません。
必要と分かれば経路の `extraHeaders` を `true` にするだけです。

### 到達不能を切り分ける

ブラウザは CORS・DNS・遮断のどれでも同じ `TypeError` を投げ、`status` を付けません。
これを `unreachable` として印を付け、HTTP エラーや中断（タイムアウト）と混ぜないように
しました。到達できないときは一度だけ通知も出します。黙って Rules に落ち続けると
「shadow にしたのに何も起きない」が原因不明のまま残るためです。

### 疎通確認のボタン

⚙→音声→判断層に「この経路へ疎通を試す」を足しました。**判断層のモードに依存しない**ので
off のままでも試せます。実際の契約と同じ形の本文を1回だけ投げ、結果を切り分けます。

|結果|意味|
|---|---|
|到達しました（N ms）|この経路は使えます|
|到達できません（CORS）|ブラウザから直接呼べない可能性。プロキシが要ります|
|HTTP 401 / 403|サーバまでは届いた。キーの問題|
|HTTP 404|Base URL と経路の問題|

**会議を1本回さないと分からなかったものが1クリックになります。**

### このヘッダ仮説は外れでした（v1.49.1 の実測）

ヘッダを外した v1.49.1 の実機ログで、**同じ 138〜139ms で同じように失敗しました。**
原因はヘッダではありませんでした。私が自信を持って書いた仮説が外れたということです。

## 外部レビューの指摘を検証した（v1.49.2）

Codex から5件の指摘を受け、鵜呑みにせず1件ずつコードで検証しました。**4件が当たり、
1件は再現せず**、検証中に**指摘より悪いもの1件**を見つけました。

|指摘|判定|実害|
|---|---|---|
|フロアゲートが未完成なのに設定上選べる|当たり（指摘より悪い）|あり|
|言語別モードが全体モードを超えられる|当たり|あり|
|probe が HTTP 200 しか見ていない|当たり|誤検知|
|往復カウンタが無い|当たり|観測不能|
|キャッシュキーに質問集の版が入っていない|当たり（現状バグらない）|将来|
|応答が二重計上される|**再現せず**|—|

### 言語別モードが全体モードを超えられた

`turnDecisionMode:'shadow'` に `turnDecisionLangJa:'active'` を足すと、日本語だけ
`active` に解決していました。**「手動で off にすれば前の HTML と同じ挙動に戻る」という
保険は、全体を下げれば必ず下がることに依存しています。**下がらないなら保険ではありません。
`RANK` による上限クリップを入れ、言語別は全体を超えられないようにしました。

### フロアゲートが「保持しない」のにタイマーを回していた

`floor()` は保持を判断する枝が存在しないのに、assist/active では `job.floorSince` を
**無条件に立てていました。**保持理由は一度も成立しないのに 3 秒後に `floor-max-wait` が
成立します。実測で TTS の待ち行列は 20〜40 秒に達するので、`turnFloorExpiry:'drop'` では
**一度も保持していない音声を捨てます。**保持理由が無い間はタイマーを回さない形に変え、
Phase 3 の実装先を `floorHold()` として分離しました。

**既存の INV-09 テストはこのバグを仕様として書いていました。**テストごと差し替えました。

### probe と往復カウンタ

probe は `r.ok` だけを見ていたので、200 を返して中身が契約と違う経路を「使えます」と
報告します。実際の `route.parse` を通し、決定が取り出せて初めて成功とするようにしました。
あわせて `requested / answered / parseFailed / stale / applied / unreachable` を
`observe()` と `pick()` で加算し、診断に「判断層の往復」行（順序 29.4）を出します。

## 疎通が確定した — 直叩きは成立しない（v1.49.3）

ブラウザは CORS・DNS・遮断のどれで失敗しても `TypeError: Failed to fetch` しか投げず
`status` を付けないので、**ブラウザ側からは切り分けられません。**非ブラウザから同じ URL を
叩くしかないので、`tools/probe-jev.ps1` を用意して Windows から実行しました。

```
-- 判定 -------------------------------------------------
  API は動いています。
  ただし CORS 応答ヘッダがありません。ブラウザからは結果を読めません。
```

**API もキーも契約も問題なく、ブラウザが結果を読めないだけ**でした。サーバが
`Access-Control-Allow-Origin` を返さないので、ブラウザは応答を破棄します。
**ブラウザからの直叩きは成立しません。**

### 管理プロキシは不要だった

以前は「後者なら管理プロキシが要る」と書きましたが、**新しいサーバは要りません。**
拡張は利用者が許可したホストに対して CORS の制約を受けないため、往復だけを拡張の
バックグラウンドに肩代わりさせれば足ります。既存の内容スクリプト（タブ音声と同じ経路）が
そのまま使えます。**第三者の中継業者も経由しません。**

経路 `jev-extension`（「Jev — アドオン経由（CORS回避）」）を足しました。

|使い方|運び方|
|---|---|
|拡張版（アドオン内のページ）|`chrome.runtime` でバックグラウンドへ直接|
|HTML 本体（GitHub Pages）|ポップアップで接続したときに張られる内容スクリプト経由|

リクエストの形は `jev-direct` と**同一**で、検査でバイト一致を確かめています。

### 踏み台にしないための縛り

何でも投げられる中継を作ると、**拡張を入れているだけであらゆるサイトの同一オリジン
制約を迂回できてしまいます。**4つ縛りました（`extension/turn-proxy.js`）。

1. 中継先は利用者がポップアップで明示的に許可したオリジン1つだけ
2. HTTPS のみ
3. 通すヘッダは allowlist だけ（Cookie も Origin も持ち込めない）
4. 頼めるのは拡張自身のページと登録済み HTML 本体のタブだけ

送信本文 256KB・応答 1MB・30 秒で打ち切り、本文も鍵も拡張側では記録しません。
中継された HTTP エラーは**ステータスを保ちます**（落とすと NEVER_RETRY が効かなくなる）。
**中継できないときに直叩きへ勝手に落ちることはありません**（落ちれば必ず失敗し、
理由が CORS に見えて原因を取り違えるため）。どちらも検査で縛っています。

鍵が DOM イベントに載る点について。同じページの他スクリプトからは読めますが、鍵は
もともと同じページの `localStorage` にあるので、そこを読める相手はイベントを待つまでも
ありません。**露出は増えていません。**

### 閾値と質問文は凍結しています

外部レビューの完了条件に同意しました。

> 固定入力に対する Jev の正常応答を1件、ブラウザ上で解析できることを完了条件にすべき

**中継は単体テストで縛りましたが、実物の応答をブラウザで解析できた実績はまだありません。**
それが出るまで閾値と質問文には手を付けません。疎通が確定していない状態での調整は
体感を変えないからです。

## 公式ドキュメントを読んで直したこと（v1.49.4）

`docs.typesafe.ai` はこの作業環境から遮断されているため、**Models** と
**Jev 1.13 jaggedness** は貼っていただいた本文で読みました。**私の実装に2つの誤りと
1つの設計問題がありました。**

### 誤り1: 文脈上限を取り違えていた

> Context length: 64k tokens per request; 32k tokens for `state` plus the longest question

**32k は OpenRouter 固有ではなく直叩きにも掛かる枠**でした。前版まで「OpenRouter は
直接の半分」と書き、**OpenRouter だけに上限を置いて直叩きには何も置いていませんでした。**

実害があります。`recentTurns` を増やせば state は伸びます。上限が無いと長い state は
`422` を受け、`422` は NEVER_RETRY なので**判断層が会議の途中で自分を off にします。**
TypeSafe 系の全経路に body 24,000 文字の保守的な上限を入れました。トークンはブラウザ
側で数えられないので文字数で代理し、全文が CJK でも 32k の内側に収まる値にしています。

### 誤り2: `active` が `jev-preview` を通していた

alias は `jev-latest` と `jev-preview` の2つで、**現在はどちらも `jev-1.13.0`** を指します。
`active` では固定 version を pin する設計なのに、**弾いていたのは `latest` だけ**でした。
両方を弾き、エラー文に `jev-1.13.0` という具体名を出します。

### 設計問題: 質問が名指ししない `state` を送っていた

> Accuracy falls as the state grows with content unrelated to the decision. Unrelated
> detail acts as a distractor. … Jev suffers from context rot.

送っている項目と、質問が バッククォートで名指ししている識別子を突き合わせたら、
**17項目のうち9項目はどの質問からも参照されていませんでした。**

|落としたもの|理由|
|---|---|
|`schemaVersion` `sessionId` `utteranceId` `revision` `speakerKey`|こちらの記録用|
|`sourceLanguage` `speechEvent` `evidence`|どの質問も参照していない|
|`candidateBoundaries`|**`criteria` 側に `before`/`after` として同じ本文が入る。丸ごと重複**|
|`tts.pendingFirstAudio` `tts.overlapMode`|`tts.active` と `tts.queueDebtMs` だけが名指し|

許可リストは**質問の本文から自動で作ります。**「文面で `` `x` `` と書けば送られ、
書かなければ送られない」という一方向の規則にしました。内部の state は全項目そのままで、
trace・local モデル・Phase 3 の床はこれらを使い続けます。

この作業中に自分のバグを1件見つけました。OpenRouter の本文で projection を先に当てて
いたため `answerSchema` から候補が消え、**選べる選択肢が `HOLD` だけ**になっていました。
「モデルは省いた値を選べない」のだから、全部 HOLD を強制するのと同じです。既存の
テストが捕まえました。

### 日本語について（公式の明記）

> English is the primary training language and where accuracy is currently best. Other
> languages, including CJK scripts, are handled but not equally well

**日英の通訳が主用途なので黙っていられません。** 設定欄の注記に出し、言語別に閾値を
持つ設計（§9.3）はこの記述と整合していることを確認しました。

### 直していないこと — 数値表現（測る前に触らない）

jaggedness の #2「Math and Numbers」に**いまの設計は正面から当たっています。**

> `jev-1.13` does not count reliably. This covers characters in a word…
> Jev will perform better on semantic representations than numeric.

`stablePrefixChars` は文字数そのもの、候補IDには文字オフセット、`silenceMs` ほかは
生の数値です。公式の指示は「コードで変換して名前付きバケットを渡せ」です。

**それでも今回は直しません。応答を1件も受け取っていないので、直しても良くなったか
分からないからです。** 前後比較の基準が無いまま表現形を作り替えるのは、外部レビューの
「接続成功を確認する前に周辺実装が先行する」の繰り返しです。trace が取れてから、
候補IDの不透明化 → 時間のバケット化 → prosody の意味化 の順で shadow 比較します。

### コストの見積り（公式の価格から）

入力 \$42/Btok、出力は無料。実測の `usage` はまだ無いので、probe の本文
（約3.7KB）から 1リクエスト 1,000〜1,500 tokens と仮定します。300ms の throttle が
効くので発話中でも最大 3.3 req/s、連続で話し続けても 200 req/min です。

|条件|1時間あたり|
|---|---|
|発話が断続的（実測の確定ペース程度）|数円〜十数円|
|連続発話で上限まで（12,000 req/時）|**\$0.5〜0.8（¥80〜120）程度**|

**アカウント単位 1,200 req/min に対して1セッションは 200 req/min** なので、単独運用では
余裕があります。**6セッション同時で上限に触ります。** トークン/秒（250,000）は
5,000 tok/s 程度で問題になりません。公式が「上限は予告なく変わる」と明記しているので、
これは現時点の値です。

### 残る作業

- **Phase 0 の未完了項目。** 話者帰属の実会議精度と遅延の実測、東京からの API 実測遅延、体感遅延の内訳分解は実会議のログが必要で未了です。recorder と replayer は揃ったので、記録を取れば算出できます。
- **Phase 2 は adapter と運搬まで。** 分解形の探索と有意性ゲートによる Go/No-Go は実データと承認が前提で未了です。まず `jev-extension` で実物の応答が1件ブラウザで解析できることを確認し、そのうえで shadow の trace を取ってから校正に入ります。
- **OpenRouter 経路は未検証のままです。** `chat/completions` 互換という前提で組んでありますが、TypeSafe が OpenRouter 上でどの入口を出しているかは確認できていません。UIにも未検証と出します。
- **Phase 1 はmodeでは戻せません。** リファクタ起因の退行は版の差し替えで戻します。出荷時に両ファイルの SHA-256 と設定既定値を一組で保存してください。
