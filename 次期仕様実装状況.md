# 次期仕様の実装状況

HTML v1.47.1 / Chrome・Edge 拡張 v1.4.8 — 2026-09-21

添付の「Teams Conference / Many-to-Many Speaker Attribution & Conference Audio Routing」に対応。Phase 1〜3 のコード実装とローカル検証を完了。**Phase 4 の実Teams会議での音声送出は v1.4.8 で確認済みです。**

|対象|実装内容|検証範囲|
|---|---|---|
|Preset|Web / Many to Many の会議Endpoint、用途説明、ホバー・フォーカス400ms、タッチ向け説明|ブラウザー確認|
|Teams Speaker Adapter|独立したDOMセンサー。字幕本文を取得せず、参加者名・ID・発話状態のみ送信|模擬Teams DOMで開始200ms・終了300ms、名前遅延、同名参加者を確認|
|Registry / Timeline|名前に依存しないID、セッション分離、epoch時刻の発話区間、ハートビート|単体・ブラウザーテスト|
|Attribution|発話時間との重なり、前400ms・後600ms補正、信頼度、複数話者・不明・判定中、遅延情報による既存カード更新|合成イベントで確認。実会議の精度は未測定|
|手動修正|カードの話者メニュー、前後話者・参加者一覧・不明・自動に戻す、手動ロックとrevision|カード表示・エクスポートを確認|
|議事録・ログ|speaker情報とrevision、議事録schemaVersion 2、指紋への反映、CSV追加列、診断ログ|単体・ブラウザーテスト。外部AIによる議事録再生成は未実行|
|Conference Audio Mode|接続中のスピーカーボタンを on → mix → off → on に変更、Drawer同期、接続初期値on、切断時リセット|ブラウザーと通信制御テスト|
|自動TTS|offで停止・待ち行列破棄、認識と翻訳は継続、再開時に過去分を再生しない|単体・ブラウザーテスト|
|Manual Replay|手動カード読み上げを会議送出対象から除外|送出ポリシー・ジョブのテスト|
|Remote Relay|既定は自分のみ。明示選択した参加者の自動・翻訳音声のみ送出可能。off/onで選択維持|送出条件を単体テスト|
|Mixer|mixだけWeb Audioで合成。元音声70%、TTS100%、各0〜100%、コンプレッサーによるピーク抑制|実Web Audio・合成音声で確認|
|Teams送出|onはTTS、offは元マイク直接。元マイク追跡、mute優先、復帰、失敗時停止|模擬senderと実Web Audioで確認|
|実Teams調整|診断項目を実装。実会議ログ5本（v1.4.2〜v1.4.6）で4件の不具合を特定し修正|**実機確認済み。on / mix / off の3モードで送出を確認**|

## 実機で確認できたこと（2026-09-21 / 拡張 v1.4.8）

- Web版TeamsへのTTS送出。**on（TTSのみ）/ mix（原音+TTS）/ off（原音のみ）の3モードすべて**で動作。
- 送信トラックの選定（`mic-in-processed-mix`）とマイクの識別。識別は体感で高速。
- 読み上げはAivisで確認。

実会議ログ5本から特定・修正した不具合は以下（詳細は `extension/history/`）。

|版|不具合|
|---|---|
|v1.4.3|出所不明のトラックを外部由来と誤判定し、候補が0件になる|
|v1.4.4|マイクと受信音声が同居する送信経路を一律に除外していた|
|v1.4.5|多段Web Audioグラフでマイクの証拠が1段で消えていた|
|v1.4.6|モード切替の2回目以降が空振りし、UIだけが新しいモードを表示していた|
|v1.4.7|ブリッジのICE収集2段分が接続期限を食いつぶし、無言でタイムアウトしていた|

## 残る実機確認

- **長い会議での接続維持。** Teamsのページ遷移でセッションが切れる事象を観測しています（ログでは89秒・111秒で切断し、合計801秒が無接続でした）。切断時に元のマイクへ戻すのは正しい動作ですが、気づきにくいため自動再接続の要否を検討します。
- 実Teamsの現在のDOMで参加者名・発話枠を検出できるか。画面構成や仮想化でタイルが存在しない場合は不明表示になります。
- 実Teamsのマイク差替え・デバイス変更・再接続。ミュート優先と復帰はコード上実装済みですが、実機での網羅確認は未了です。
- STTの発話区間とDOMイベントの実遅延。録音単位のSTTや連続発話のカード分割は概算時刻を使うため、短い交代や発話重複の精度は実ログで調整が必要です。
- Aivis以外のTTSサービスでの会議中の確認。
- 実ブラウザーによる自動E2E。`extension/tests/test-browser.cjs` は playwright が必要で、成功実績がありません。今回の確認はすべて人手による実機操作です。

真の音声分離・声紋識別は実装対象外です。混ざった音声から個別の音声を分離する機能ではありません。話者メタデータが取得できなくても認識・翻訳は継続します。

検証結果の詳細は `検証結果.json`、更新方法は `更新手順.md` を参照してください。

## Jev適用 Phase 0 / Phase 1（2026-09-21）

開発仕様書 v2 の Phase 0（計測基盤）と Phase 1（契約と parity）を実装。**本番動作は変わりません。**
`turnDecisionMode` の既定は `off` で、そのとき境界判定と Floor Gate は v1.47.1 と同一の値を返します。

|対象|実装内容|検証範囲|
|---|---|---|
|判断層の契約|`TurnDecision`。`{length, reasons, waiting}` を正規化契約とし、既存 `segDecision` / `segSemanticDecision` を RulesProvider として常時搭載|単体28件|
|OFF互換|`turnDecisionMode=off` で `boundary` が RulesProvider の戻り値を**同一参照で**返す。言語別設定を active にしても変わらない|単体（参照一致で検査）|
|INV-08|判断の不在はゼロコスト。cache に結果が無ければ待たず Rules へ進む|単体|
|INV-09|Floor 待ちは `turnFloorMaxWaitMs`（既定3000ms）を超えて保持しない。期限時は speak または drop|単体|
|ProsodyAnalyzer.peek|発話途中の非破壊スナップショット。`finalize()` は frames をリセットするため判断層からは呼べない。終端ピッチ勾配・エネルギー減衰・ポーズ構造を相対値で返す|単体（非破壊性を検査）|
|prosody のゲート|`turnDecisionMode=off` では peek を呼ばない。60ms周期のYIN抽出を無駄に走らせない|単体（呼び出し回数0を検査）|
|silenceMs の契約|meter不明を `null` で渡す。Rules内部の `-1` は契約へ漏らさない|単体（0/null/-1 を区別）|
|境界候補の安全性|候補は安定prefix以内。候補外offsetと安定prefix超過は採用しない|単体（注入offsetを拒否）|
|speakerKey|Teams speakerId は session単位のHMAC相当で仮名化。平文IDを外部へ出さない。無ければ seat|単体|
|送信の最小化|`turnDecisionContextTurns` の既定は 0。原文は `turnDecisionRawLog` が明示onのときだけ記録|単体|
|カウンタ分離|`SEG.committedBySource` / `correctedBySource`。判断層をoffへ戻したあとの累積比の残留と baseline 汚染を切り分ける|単体|
|turn trace recorder|`turnTraceMode=record` のときSTT・VAD・話者・first audio・Rulesの決定を時刻付きで記録。off では1行も積まない|単体|
|replayer と自動ラベル|`extension/tools/turn-trace-replay.js`。記録時のRulesの決定を再現できるか検査した上で、premature commit を自動ラベルで数える|単体8件|
|2ファイル同期|`index.html` と `extension/app.js` の seg系・判断層・設定既定値が空白無視で一致することを検査|単体19件|
|テストランナー|`extension/tests/run-all.cjs`。決定論的スイートをgate、実ブラウザE2Eを別扱い|全スイート実行|

### 実ブラウザ自動E2Eが通るようになりました

これまで `extension/tests/test-browser.cjs` は成功実績がありませんでした。原因は2点で、いずれも製品側ではなくテスト側の問題でした。

- ICE gathering の **完了** を待っていた。STUN へ到達できない環境では完了しないため必ず5秒で落ちる。製品側の中継は v1.4.7 以降は最初の候補で応答する仕様（`test-bridge-target.cjs` の契約）なので、テストも候補1つで進めるようにした。
- mDNS で隠された `.local` 候補は同一ページ内のループバックで解決できない。`--disable-features=WebRtcHideLocalIpsWithMdns` で実IPを出させた。

実行方法（このリポジトリには package.json が無いためグローバル解決に頼ります）。

```
node extension/tests/run-all.cjs                                  # 決定論的スイート
NODE_PATH=$(npm root -g) node extension/tests/run-all.cjs --with-e2e
```

### Web Speech でタブ／システム音声が取り込めなくなっていた不具合の修正

Web または Many to Many プリセットで音声認識に Web Speech を選ぶと、開始時に
「Webプリセットの共有音声にはOpenAI / Groq / Geminiの音声認識を選んでください」で止まっていました。
v1.47.1 から存在する不具合で、判断層とは無関係です。原因は2つあります。

- **capability テーブルが実装と矛盾していた。** `STT_ADAPTER_CAPABILITIES.webspeech` が
  `arbitraryTrack:false` と宣言していましたが、`WebSpeechTrackEngine` は Chrome 135+ の
  `SpeechRecognition.start(audioTrack)` で共有音声 Track を直接認識するために存在します。
  能力はあるのにテーブルが「無い」と言っていました。
- **判定が広すぎた。** `duoValidateInputs()` は system-audio の endpoint が *有効かどうか* だけを
  見ており、それを **認識源に使うのか** を見ていませんでした。Web プリセットでマイクを認識源にして
  会議音声は TTS の送出先にしか使わない構成まで巻き込んで止めていました。

修正後は、A／B のどちらかが認識源として共有音声に束ねられている場合だけ検査し、経路ごとに
必要な能力を見ます。`direct` は Track を直接渡すので `arbitraryTrack`、`api` は Blob を送るので
`systemAudio`、`vb` は仮想入力デバイスなので通常のマイク扱いでどの adapter でも通ります。
開始できない実機は `WebSpeechTrackEngine` 側が理由を出して止まるため、テーブルで先に塞ぎません。

`extension/tests/test-input-validation.cjs`（10件）で組合せを固定しました。

### 設定 UI を追加しました

⚙→音声 に「発話交代の判断層（実験）」を追加しました。判断モード、言語別の上書き、音響特徴の送信、
送る直近ターン数、読み上げ開始の待ち上限と期限時動作、trace 記録、原文の記録、trace の書き出しと破棄、
状態行を持ちます。DevTools での `localStorage` 操作は不要になりました。

markup は `index.html` と `extension/app.html` の両方へ置くと同期先が3つになるため、`duoNextInstall` と
同じく **JS から注入**しています。`bindSettings()` は読み込み時に一度だけ走るので、注入後に `simple()` を
呼んで既存の永続化経路へ相乗りし、独自の onchange は書いていません。

Phase 2 まで意味を持たない設定（provider / model / baseUrl / apiKey）は UI に出していません。
`localStorage` で設定できますが、向ける先がまだ無いためです。

`extension/tests/test-settings-ui.cjs`（8件）で実ブラウザでの生成・初期値反映・永続化・記録の
開始停止・状態行・page error なしを固定しました。

### Phase 2 の Provider adapter 層

`TurnProviders` を追加しました。判断契約を Duo が所有し、ベンダーを adapter で差し替えます。
既定の `turnDecisionProvider='rules'` では registry を一切引かないので、**shadow でもネットワークは発生しません。**

|要素|内容|
|---|---|
|正規化|schema外の選択肢、提示していない候補への確率、確率合計の破綻、NaN・範囲外・型違い、巨大応答、非JSONを必ず拒否。通らなければ捨てて Rules へ|
|`jev-direct`|`POST /v1/systemone`。公式リファレンスどおり `{state, model, questions}` を送り、`answers` を自分の質問IDで引く。ZDR と no-training をヘッダで明示送出。HTTPS のみ許可。キー未設定なら要求前に拒否。active では model の alias を拒否（§8.5 の version pin）|
|`local`|ブラウザ内の合成器。遅延ゼロ・外部送信なし・レート制限なし。**学習済みの重みが無いあいだは動きません**|
|circuit breaker|provider＋言語単位。30秒内3回の障害で OPEN 60秒、half-open は成功2件で復帰|
|発火制御|speakerKey ごとに同時1件、新 revision で abort、同一 bucket は300ms以内再送しない。音響 bucket は100ms刻みで量子化|
|401／422|設定エラーとして自動再試行せず、判断モードを off へ戻して通知|
|`questionSetHash`|instructions 本文のハッシュ。文面が変われば校正記録を無効として扱う|

`local` が手置きの係数で動かないのは意図的です。仕様書 §9.1 は「閾値を手置きせず学習する」と
定めており、係数を決め打ちすると校正の意味が消えます。重みは trace から学習します。

```
node extension/tools/turn-trace-replay.js turn-trace-....json --fit --out weights.json
```

**言語ごとに独立して**自動ラベルからロジスティック回帰で重みを当て、前半で学習・後半で holdout
評価し、**precision 目標から閾値を逆算**します（既定 0.98、`--precision` で変更。最低件数は `--min`、
既定50）。目標に達しない言語、件数が足りない言語、holdout が片方のラベルだけの言語は `skipped` に
理由付きで出し、重みを出しません。**片方の言語だけ合格することがあります。**

言語を分ける理由は、公式ドキュメントが「日本語の本番ワークロードは英語の閾値を流用せず独自の
ラベル付き評価が必要」と明記しているためです。話者Aが日本語・話者Bが英語の会議 trace を1つの
モデルへ混ぜると、この注意をそのまま踏み抜きます（仕様書 §9.3）。

出力は `{version, languages:{ja:{...}, en:{...}}}` 形式で、⚙ の設定または `localStorage` の
`di.tdWeights` へ入れると `local` が動きます。`local` は発話の src 言語で重みを引き、
**その言語の重みが無ければ他言語のものを流用せず拒否します。** 閾値も学習時に逆算した値を使い、
実行時に決め打ちしません。

`test-turn-providers.cjs`（22件）で拒否すべき応答の網羅、ZDR ヘッダ、circuit breaker、
発火制御を固定しました。`test-turn-trace-replay.cjs` に学習側の5件を追加し、
**学習側と推論側の特徴量の並びが一致すること**も検査しています（ずれると係数が別の特徴へ当たる）。

実装中に自分のテストが1件バグを見つけました。`Number(null)` が 0 になるため、欠落した Noul 値が
「確率0」として通っていました。読み上げを永久に抑止したり誤って開始したりする経路だったので、
型で弾くように直しています。

### API 契約を公式リファレンスに合わせました

Phase 2 の初版は `docs.typesafe.ai` に到達できず検索結果から組んだため、リクエストとレスポンスの
形状が間違っていました。公式リファレンスを突き合わせて直しました。

|直した点|誤り|正しい契約|
|---|---|---|
|instructions の位置|トップレベルに1つ|**question ごと**に `instructions`|
|choice の選択肢|`options` 配列|**`criteria`** の「選択肢 → 説明」map（最大255）|
|noul の criteria|送っていない|任意だが `{true, false}` で yes/no の意味を書ける。送るようにした|
|`model`|未設定なら省略|**必須**。既定 `jev-latest`、active では alias を拒否|
|レスポンス|`{boundary, turnState, safeToSpeak, ...}` を直接期待|**`answers` を自分の質問IDで引く**。choice は `choice`／`probabilities`／`confidence`、noul は `noul` のみ|
|`usage`|捨てていた|`input_tokens` を残す。レート制限がアカウント単位なので実測が必要|
|never-retry|401／422 のみ| 400／401／403／404／422。既知の不一致として本来422の検証エラーが400で返ることがある|

**旧実装のままでは、正規化が実際の応答をすべて拒否していました。** 契約テストで固定しました。

あわせて**候補IDの重複バグ**を直しました。`criteria` は map なので、同じIDが2つあると衝突して候補が
消えます。旧実装は文末が複数ある文章で `C_SENTENCE` を重複生成していました。offset ごとに一意な
IDを振り、左右24文字の文脈を添えてモデルへ意味を渡すようにしています。同一 offset の候補は
重複排除されます。

**instructions は state のフィールドをバッククォートで参照して意味を書いています。** Jev はテキストしか
見ないので、`prosody.terminalPitchSlope: -0.25` のような数値は説明がなければ解釈できず、ノイズとして
送られるだけになります。初版はこれを書いていませんでした。あわせて Provider へ送る prosody を
`TURN_PROSODY_SENT` の6フィールドに絞り、**送るのに説明のないフィールドがあればテストが落ちる**ように
しました。`silenceMs` の null が 0 と別であることも instructions に明記しています。

**instructions と criteria は英語で書いています。** 公式が「英語が最も強い言語で、CJK は受理される
が精度は同等でない」と明記しているためです。判断対象の日本語は `state` と候補の左右文脈として
原語のまま渡るので情報は失われません。この選択自体は shadow で検証します。

### 境界の閾値を confidence から probabilities へ移しました

公式の `/confidence` に算出式が載っていました。`confidence` は確率分布から導く統計量で、
**選択肢数 N に依存します**（ドキュメントの近似式は `(N × p_max − 1) / (N − 1)`）。

`boundary_choice` の N は HOLD ＋ 候補1〜5件で**リクエストごとに変わります**。同じ `p_max = 0.7` でも
N=2 なら 0.40、N=6 なら 0.64 です。つまり仕様書 §6.4 の「boundary confidence 下限 0.60／0.70」は、
モデルの確信度ではなく **Duo が何件候補を作ったか**で合否が変わる閾値でした。設計として成立しません。

そこで境界の判定を `probabilities` 側へ移しました。候補数に依存しない量だけを見ます。

|判定|使う値|理由|
|---|---|---|
|HOLD するか|`P(HOLD)` の上限（en 0.15 / ja 0.10）|候補数に依存しない|
|commit するか|選ばれた候補そのものの確率の下限（en 0.70 / ja 0.80）|同上|
|発話継続|`turn_state` の `confidence` 下限 0.50 と `P(CONTINUING)` 上限|**選択肢が4つ固定**なので confidence が比較可能|
|言い直し|`repair_likelihood` の上限（en 0.20 / ja 0.12）|Noul は値そのものが確率|
|音響 guard|最低無音（en 250ms / ja 450ms）。meter 不明は STT final まで待つ|null と 0 を区別する|
|ヒステリシス|rule と不一致なら N 回連続で同じ offset を支持したときだけ commit（ja は2回）|閾値付近の振動で確定させない|

`UNKNOWN` は Rules へ落とします。公式が「Noul の 0.5 は中くらいではなく判断がつかない」と
書いているのと同じ扱いです。

N 依存そのものをテストに残しました（`the documented confidence statistic moves with the option count`）。
固定 0.60 の下限が同じ確信度を候補数で割ってしまうことを数値で示しており、将来 confidence 下限へ
戻そうとしたときにその記録が残ります。

**これらは校正前の出荷値です。** §9.1 の学習で置き換える前提で、ASSIST 以上へ上げる前に校正が必要です。

### 残る作業

- **Phase 0 の未完了項目。** 話者帰属の実会議精度と遅延の実測、東京からの API 実測遅延、体感遅延の内訳分解は実会議のログが必要で未了です。recorder と replayer は揃ったので、記録を取れば算出できます。
- **Phase 2 は adapter と学習経路まで。** 分解形の探索と有意性ゲートによる Go/No-Go は実データと承認が前提で未了です。Jev の early access キーと、第三者へのデータ送信の承認が揃ってから shadow 比較に入ります。
- **Phase 1 はmodeでは戻せません。** リファクタ起因の退行は版の差し替えで戻します。出荷時に両ファイルの SHA-256 と設定既定値を一組で保存してください。
